# 项目目标与验收规范

> 本文件是项目目标、范围与验收标准的唯一正式来源。
> `PROJECT.md` 继续保存技术决策、当前进度与演变历史，但不得覆盖本文件中的目标和边界。
> 产品目标发生变化时，由项目所有者确认后更新本文件；Agent 不得自行扩大范围或降低标准。

## 项目概述

- 项目：Vicious-W 的个人主页。
- 形态：部署在 GitHub Pages 的纯前端静态网站，无后端、数据库或账号系统。
- 定位：用强表现力、视觉冲击力和有趣交互展示所有者的个性、艺术追求与项目经历。
- 设计基调：以蓝色为全站主色，结合几何、数学、自然与实时图形元素，追求简约、高级、具有生命感的体验。

## 最终目标

1. 保持单页四屏结构，并完成前三屏的全站蓝色主题重构；第四屏保留并继续使用已经建立的真实水塘路线。
2. 第一屏呈现“透过独立玻璃立方体俯视核反应堆重水池”的实时场景：底层反应堆、中层重水、表层玻璃立方体都具有可辨认的体积、光学或物理特征。
3. 第四屏呈现夏日雷阵雨后的真实小水塘：蓝天积云倒影、水面波动与高光、水下生态、小鱼及指针涟漪共同形成有深度的实时场景。
4. 在桌面、平板与移动端均保持可访问、可滚动、无明显布局破坏的核心体验；实时图形不可用时提供合理降级。
5. 主观视觉是否达到预期由项目所有者最终判断；Agent 的审查结论必须依据本规范、所有者的明确反馈和可复现证据，不能用个人审美替代验收标准。

## 当前阶段

- 当前活动任务见 `.agent/next-task.md`：第一屏进入“玻璃层物理/材质/声音重做 + 反应堆层真实三维重建”阶段。这是路线级重建，不是继续给现有平面图案调参数。
- 第一屏文字和旧 Cantor 图当前按所有者决定临时隐藏；在所有者确认文字回归方案前，这不是缺陷。
- 当前玻璃立方体已经是独立 Three.js 网格和 cannon-es 刚体，但所有者确认其手感偏飘、材质不够真实且没有物理声音；下一阶段必须同时解决这三项。
- 当前反应堆主体仍由平面 shader 表现，第一眼像贴图。正式主体必须改成依据公开可靠资料建立的独立三维部件和可信运行状态；原 shader 只保留为首屏瞬开/失败降级。
- 第四屏水塘成果保留，当前阶段不主动扩大其功能范围，只处理明确回归或所有者的新反馈。

## 主要用户流程

1. 用户打开首页，首屏立即显示重水池兜底画面；Three.js 模块加载成功后由独立玻璃立方体场景平滑接管。
2. 用户使用鼠标或触控指针抓取第一屏玻璃立方体，移动后松开；立方体受物理约束影响并可落在其他立方体上。
3. 用户纵向滚动浏览 About、Selected work 和 Contact 四个区块。
4. 用户通过 Selected work 链接访问 `VKC_Study` 或 `c-learning`，通过 Contact 区域打开邮件或 GitHub 链接，并可返回顶部。
5. 用户在第四屏移动鼠标或触控水面，看到涟漪传播、衰减并影响倒影、折射和鱼的行为。

## 必须实现

### 页面与内容

- 保留 Hero / About / Selected work / Contact 四个区块以及现有核心链接和联系信息。
- 全站最终统一到蓝色主题，同时维持页面之间可区分的构图与表现力。
- 当前重构阶段不得误删暂时隐藏的第一屏文字和语义结构。

### 第一屏

- 首屏使用轻量原生 WebGL1 重水池作为即时画面和 Three.js 加载失败时的兜底。
- 玻璃层由独立的三维网格组成，不得退回在整屏 shader 中划格模拟物体的路线。
- 立方体需要有真实体积、透明/折射观感、刚体碰撞和指针拖拽/放下能力；质量、惯性、重心、阻尼、摩擦、弹性、约束和休眠共同产生稳定、有重量、可堆叠的手感，不得呈现无故漂浮或长时间抖动。
- 玻璃声音必须由实际物理事件驱动：碰撞冲量、相对速度、接触位置和运动状态决定撞击/落地/滑动声音的强弱与空间位置；不得用与碰撞无关的固定循环音效。音频在首次用户手势后启用，并限制并发、重复触发和峰值音量。
- 正式反应堆主体必须是可辨认的三维场景图。实现者先依据公开、可靠、可引用的真实研究堆资料确定与当前俯视水池构图相符的原型，再分别建立池体/井壁、堆芯支撑或栅格、燃料组件、控制棒及驱动机构、冷却/循环相关结构和其他原型所必需的主要部件。
- 反应堆主要部件需要有独立网格、材质、状态和运行逻辑；控制机构、水流/扰动、辉光和可见仪器活动之间应具有可解释的状态关系。仅把旧 shader 贴到三维平面、把所有部件合并为一个模型或播放互不关联的循环动画，均不算完成。
- 网站中的“真实运行”指结构关系、尺度层次、机械运动、材料、光学和可见状态变化可信；本项目不声称提供工程级中子输运、热工水力或安全分析仿真。
- 第一屏不恢复已经取消的鼠标视差。
- Windows/ANGLE 兼容优先；共享池面 shader 以 WebGL1 为兼容基线，除非有明确且经过验证的升级理由。

### 第四屏

- 继续采用“原创写实生态底图 + 实时水膜”的混合渲染路线，不退回用单个 fragment shader 程序化生成全部植物、鱼和云。
- 水面需要同时呈现倒影、透明感、波纹、高光与轻微扰动；水下植物和鱼可被察觉但不过度抢戏。
- 指针/触控涟漪需要传播并衰减，同时影响水面倒影与水下折射；鱼对附近扰动做克制避让。
- 保留离屏暂停、DPR/帧率限制、静态底图和 WebGL/context lost 降级。

### 工程与部署

- 保持 Vite + Three.js + 原生 GLSL + cannon-es 的现有技术路线。
- 生产构建必须输出可由 GitHub Pages 托管的静态文件，资源基址保持兼容相对路径部署。
- 页面脚本不得依赖后端服务、运行时密钥或数据库。

## 非目标

- 后端、数据库、账号、登录、CMS、支付、线上写入或管理后台。
- 未经所有者要求的框架迁移、WebGPU 主路线、国际化、暗色模式或新页面体系。
- 当前阶段主动扩充第四屏的浮叶层、复杂鱼群或新的生态功能。
- 为满足审查而进行与明确目标无关的重构、依赖升级或性能微优化。
- 无上限、可并发、会自动 push/部署或在异常后无限重试的双 Agent 循环；最多三轮的本地串行自动闭环属于正式工作流。

## 不允许改变

- 不得改变“静态站 + GitHub Pages”部署前提。
- 不得删除四屏结构、现有项目链接、联系方式或实时场景降级能力。
- 不得把第一屏玻璃对象改回二维分格假象，或把第四屏改回全程序化生态 shader。
- 不得删除失败检查、降低验收标准或把缺少的检查伪装成通过。
- 不得覆盖所有者未提交的修改，不得自动 push、强制 checkout、rebase、reset、clean 或部署。
- Claude Code 是业务代码的唯一实现者；Codex 常规阶段只审查并写入约定的报告/日志路径。

## 验收标准

### 构建与静态部署

- `npm run build` 退出码为 0。
- 构建产物可通过静态服务器打开，首页及四个锚点区块可访问。
- 当前未配置自动测试、lint 和类型检查；统一验证结果必须明确显示 `NOT CONFIGURED`，不能显示为通过。

### 页面与主要流程

- 首页正常打开，无未处理的 JavaScript error 或 WebGL shader 编译错误。
- 四屏可纵向浏览，核心链接存在且可操作；第一屏拖拽和第四屏涟漪在支持的设备上可用。
- 第一屏玻璃块释放后能快速形成稳定接触或堆叠，不出现明显悬停、持续弹跳、穿透、爆炸式速度或与指针脱节；拖拽、碰撞和落地能触发与物理强度相符且不过载的空间声音。
- 第一屏正式场景中能从改变视角、遮挡、光照和独立运动确认反应堆主要结构是真实三维部件而非平面图案；至少一种控制机构运动会引起水体/辉光或相关可见状态的合理联动。
- 实时图形加载失败时页面仍保留可见背景和基本内容，不出现整屏空白或页面崩溃。
- 对页面外观或行为的任何修改，都必须通过 Playwright MCP 实际渲染检查，而不是只读代码判断。

### 响应式

- 至少检查 `390 × 844`、`768 × 1024`、`1440 × 900` 三种视口。
- 不得出现明显横向溢出、核心文字/链接不可见、区块互相遮挡或画布尺寸为零。

### 动效、性能与可访问性

- `prefers-reduced-motion: reduce` 下不得持续运行非必要动画；静止模式应保持可用画面。
- 重型 Three.js 场景继续延迟加载，不应阻塞首屏的轻量兜底画面。
- 页面保留语义标题、导航标签和装饰 canvas 的 `aria-hidden` 处理。
- 当前未设定硬性的 Lighthouse 分数或包体积阈值；性能退化只能在有可比较证据且影响主要流程时判为阻塞问题。

## 项目命令

| 用途 | 命令 | 状态 |
| --- | --- | --- |
| 安装依赖 | `npm install` | 已配置 |
| 本地开发 | `npm run dev -- --port 8000` | 已配置 |
| 生产构建 | `npm run build` | 已配置 |
| 预览构建 | `npm run preview -- --port 8000` | 已配置 |
| 统一验证 | `./scripts/run-validation.sh` | 已配置 |
| Claude 实现 | `./scripts/agent-cycle.sh implement` | 已配置；要求干净工作区 |
| Codex 审查 | `./scripts/run-review.sh` | 已配置 |
| 自动闭环 | `./scripts/agent-cycle.sh cycle` | 已配置；串行、最多三轮、不 push |
| 测试 | — | `NOT CONFIGURED` |
| lint | — | `NOT CONFIGURED` |
| 类型检查 | — | `NOT CONFIGURED` |
| CI | — | `NOT CONFIGURED` |

## 相关文档

- `PROJECT.md`：技术决策、当前状态和追加式历史记录。
- `REVIEW_CONTRACT.md`：Codex 审查格式、等级和终止条件。
- `CLAUDE.md`：Claude Code 实现者规则。
- `AGENTS.md`：Codex 审查者规则。
- `.agent/README.md`：双 Agent 交接文件与日常操作说明。
