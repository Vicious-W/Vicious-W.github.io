# 当前目标与验收规范

本文件是当前产品目标、范围和验收标准的唯一正式来源。详细工程过程分别由
`docs/engineering/FLY_PHYSICS.md` 与 `docs/engineering/FLY_SCENE_ARCHITECTURE.md`
约束；它们不能覆盖本文件锁定的所有者决定。

## 1. 产品定义

- 网站保持单页、全屏、纯前端静态构建；
- 网站入口先提供 `SOURCE` 与 `FLY` 两个三维场景的选择；
- `SOURCE` 是已实现、已验收的稳定场景；
- `FLY` 是当前唯一开发目标；
- 蓝色是全站核心颜色，但自然天空、云、地表、火焰和飞行器必须保留真实材质与光色；
- 不使用个人介绍、传统导航、内容区或说明性文案；
- FLY 只允许两类必要文字：飞行器操作指南，以及真实设备原生标签、刻度、警告和
  仪表数字；
- 主要内容必须通过三维几何、物理状态和真实过程建立，不能用图片、背景视频或预录
  动画代替。

## 2. 当前任务

任务 ID：`fly-foundation-hot-air-balloon-v1-2026-07-31`

建立 FLY 的第一个完整纵向切片：用户从全站入口进入 FLY，在晴朗天气和 Cameron
C-100 参考热气球已选中的出发空间阅读操作指南，手动控制燃烧器和放气完成离地、
升降与随风航行，并可启动符合热气球能力的自动安全降落和回收。

该纵向切片必须同时建立以后可复用的多场景生命周期、统一物理时钟、世界坐标、标准
大气、风场、程序化区块、物理飞行器接口和状态音频边界。不得把首个热气球写成以后
无法扩展的孤立演示。

## 3. 已锁定决定

### 3.1 场景和体验

- 第二场景正式名称为 `FLY`；
- 每次打开或刷新网站先进入场景选择空间，不再直接启动 SOURCE；
- 场景选择本身无文字，使用实时三维缩影、运动、光色和声音区分 SOURCE 与 FLY；
- 进入 FLY 后先选择气象和飞行器；第一阶段只有晴空与热气球可用，但选择注册结构
  必须允许后续增加天气和飞行器而不重写场景主循环；
- 热气球选择完成后显示型号专属操作指南；用户主动确认出发后才解锁音频并运行旅程；
- 用户默认是驾驶员并手动驾驶；不提供自动巡航代替基本驾驶；
- 旅程中提供自动安全结束功能。热气球不能获得虚构水平推力，也不能强行返回起点；
  自动系统应寻找由当前风层可达的安全区域，控制高度并真实着陆；
- 用户可以通过无文字入口或 `Escape` 返回全站选择空间；返回会销毁当前场景会话，
  再进入时建立新会话。

### 3.2 文字边界

- 全站场景选择、FLY 气象选择、出发装置和返回入口均不使用文字；
- 操作指南可以显示飞行器名称、按键/指针映射和必要安全说明；
- 驾驶舱、设备、仪表和警告标记可以保留真实原型自带的必要文字与数字；
- 不借“操作指南”例外增加欢迎语、背景故事、功能营销或普通导航。

### 3.3 统一物理引擎

- FLY 只有一个权威仿真时钟和一个权威世界状态；
- 大气、气象、刚体、空气动力、推进、热力、结构、碰撞、声音和粒子是模块，不是
  各自拥有时间与真相的独立动画；
- 渲染帧只能消费插值状态，不能决定燃料消耗、浮力、风场或着陆结果；
- 真实飞行器类别使用独立物理模型；热气球、固定翼、喷气式、旋翼、航天飞机和 UFO
  不得共享一套换皮运动函数；
- UFO 属于未来虚构模型，必须使用内部一致的工程规则并明确标记，不能伪造现实来源；
- “无限”通过确定性程序化区块、局部活动物理域、分级细节和浮动原点实现，不创建
  无限刚体，也不声称进行了全球直接数值天气模拟。

## 4. 多场景架构要求

- `src/main.js` 只负责创建全站宿主，不再直接永久拥有 SOURCE；
- SOURCE 与 FLY 实现统一的 `mount/start/pause/resume/dispose` 生命周期或等价明确
  契约；
- 任意时刻最多存在一个活动场景会话和一个活动物理时钟；
- 场景切换必须清理 requestAnimationFrame、定时器、DOM 监听器、指针捕获、Web Audio
  节点、刚体、约束、Three.js 几何/材质/纹理/渲染目标和调试接口；
- SOURCE 的反应堆、轻水、玻璃、声音、相机和 AUTO/MANUAL 行为不得因宿主改造回退；
- 场景之间不共享可变业务状态；共享代码只提供时间、数学、资源管理、输入和抽象接口；
- 可以提供仅供测试的 URL 场景参数，但普通用户入口仍是无文字三维选择空间；
- 浏览器后退、窗口失焦、标签页隐藏和 WebGL 恢复不能同时恢复两个场景。

## 5. FLY 世界与大气

### 5.1 坐标与无限生成

- 使用世界级逻辑坐标记录航程，使用靠近相机的局部坐标进行 Three.js 和刚体计算；
- 飞行器远离局部原点时执行浮动原点平移；平移前后高度、速度、姿态、风采样、燃料、
  轨迹和相机相对关系连续；
- 前方按确定性种子生成区块，后方区块安全回收；相同种子与世界坐标产生相同地形、
  水体、障碍、风场和候选降落区；
- 远景允许使用 LOD、实例化和解析地平线，但地面、海洋、山体、树木和主要云体不能
  用一张背景图片冒充；
- 只有飞行器附近、可能碰撞或参与降落的对象进入活动刚体域；远方对象不得浪费高频
  刚体求解。

### 5.2 标准大气与晴空

- 基础温度、压力和密度随高度变化，使用 U.S. Standard Atmosphere 1976 或等价正式
  标准的分层模型；
- 局部天气在标准大气基线上增加温度、压力、湿度和风的扰动，不能分别制造互相矛盾
  的密度值；
- 所有飞行器统一从 `atmosphere.sample(worldPosition, time)` 或等价接口取得温度、
  压力、密度、湿度、风速、湍流和降水状态；
- 第一阶段晴空仍包含随高度变化的背景风、缓慢阵风和由地表受热产生的局部热气流；
- 风作用于飞行器的依据是相对空气速度 `v_air = v_body - v_wind`，不是世界速度；
- 天空光色、地平线衰减、远景能见度、稀疏晴空积云和风声读取同一大气状态；
- 体积云使用三维密度和光学状态，不用云照片或平面云片作为主要云体。

## 6. 首个飞行器：热气球

### 6.1 原型和真实性标签

- 包络几何以 Cameron C-100 官方规格为第一参考：16 个 gores、体积 100,000 ft³、
  高度 65 ft、直径 57 ft；对应公制值约为 2,832 m³、19.81 m、17.37 m；
- 官方页面给出的 certified weight 为 2,000 lb，standard envelope weight 为 218 lb；
  实现中必须区分最大认证重量、包络自重和实际起飞总质量，不能把三者混为一谈；
- 首阶段下部系统使用同一 Cameron 产品族的篮筐、框架、NEO 燃烧器和燃料瓶作为参考；
  若未取得完整型号兼容表，必须标记为 `FLY_REFERENCE_CONFIGURATION`，不得声称是某架
  认证航空器的精确复刻；
- 任何具体尺寸、质量、燃料流量和热功率都要在模型清单中记录来源或工程代理标签。

### 6.2 独立三维部件

至少分别建立：

- 有真实体积和纵向分片的织物包络；
- 顶部 parachute/deflation vent、拉索和可见连接；
- 纵向承力带、口部耐热面板、悬索/飞行索；
- 承载框架与燃烧器罐体、阀门、火焰出口；
- 两只独立燃料瓶、阀门、软管和燃料状态；
- 有壁厚、编织结构和底部碰撞形状的篮筐；
- 驾驶员位置、质量代理和可交互物理控制件。

不能用一个椭球加一个盒子代表完整热气球，也不能让所有部件只是跟随同一动画矩阵。
包络与吊篮应有可观察的相对摆动和载荷传递；允许用低自由度约束代替逐根绳索有限元，
但等效质量、阻尼和力臂必须明确。

### 6.3 热力、浮力和运动

- 外部浮力按排开空气质量计算：`F_b = rho_out * V * g`；
- 系统重量至少包含包络、框架、燃烧器、篮筐、燃料、驾驶员和内部热空气；
- 内部空气密度由压力与温度状态计算，不能把“燃烧器开启”直接映射为上升速度；
- 燃烧器先消耗燃料并增加内部空气能量，温度经热损失和通风演化，密度变化后才改变
  净浮力；
- 停止燃烧后包络自然冷却；拉动顶部 vent 增加热空气排出和下降趋势；
- 包络、篮筐和悬挂系统分别承受相对风阻、阵风和接触力；
- 水平运动主要来自风层和气动阻力。用户只能通过升降寻找不同风层改变航迹，不能用
  `A/D` 或摇杆直接产生虚构侧向推力；
- 燃料质量随使用单调减少，并反馈总质量、可用航时和自动降落决策；
- 起飞必须经历净浮力建立、地面法向力下降、离地和悬挂系统收敛；不得直接设置高度；
- 着陆必须由地形碰撞、篮筐速度、阻尼、倾倒/拖曳可能性和包络卸载共同形成；不得
  播放预录落地动画。

### 6.4 手动控制与指南

- `Space` 或对应物理手柄：按住主燃烧器，松开关闭；
- `V` 或对应控制绳：按住顶部 vent，松开恢复；
- 鼠标/触控允许直接操作真实手柄与拉索；
- `R` 或无文字三维回收控制件：请求自动安全降落；
- 操作指南必须说明热气球没有直接水平操纵，方向来自不同高度的风；
- 指南在出发前显示，可在旅程中通过无文字帮助图标重新打开；打开指南时物理时钟
  明确暂停或继续，不能出现不确定状态；
- 键盘失焦、标签页隐藏、pointercancel 或 pointer capture 丢失时，燃烧器和 vent
  持续输入立即归零。

### 6.5 自动安全降落

- 自动系统只在用户明确请求后接管燃烧器和 vent；
- 使用当前风层、地形坡度、地表类型、障碍、预计接地点、下降率和燃料余量评估候选点；
- 通过真实升降和随风漂移接近候选点，不改写世界坐标、速度或风场；
- 水面、陡坡、密集树木、建筑和输电设施不得被标记为安全着陆区；
- 若首个候选点变得不可达，应继续飞行并重新选择，不能瞬移修正；
- 篮筐接地并稳定、燃烧器关闭、包络卸载后才进入 `RECOVERED`；随后用户可以返回
  FLY 选择空间。

## 7. 相机、视觉和声音

- 默认驾驶员视角位于篮筐内，允许自然环视但不能穿出飞行器获得不公平操纵信息；
- 提供外部跟随和环绕观察，但切换相机不改变刚体、风场或控制状态；
- 地平线、太阳方向、大气散射、云影和地表尺度必须共同提供高度与速度参照；
- 燃烧器火焰来自阀门、燃料流量和热功率；关闭阀门后不得继续循环播放火焰；
- 音频只在用户手势后创建或恢复；
- 燃烧器、whisper burner（若实现）、相对风、织物拍动、悬索受力、篮筐接触和落地
  分别拥有状态驱动的声源；
- 雷声、暴雨和台风声音属于后续天气，不在晴空阶段伪造；
- `prefers-reduced-motion` 只减少非必要展示动画，不能改变权威物理结果或隐藏控制反馈。

## 8. 工程约束

- 保持纯前端静态构建、无账号、后端、数据库和运行时密钥；
- 复用 Three.js、Web Audio 与现有构建体系；是否继续使用 cannon-es 或增加专用模块
  必须由明确技术问题决定；不得同时保留两个互相争夺飞行器状态的权威刚体世界；
- 核心物理使用固定时间步和有上限的追帧子步；页面长时间隐藏后不得一次补算全部时间；
- 天气和远景可用较低频率更新并插值，但飞行器力必须读取时间一致的采样；
- 所有随机过程使用显式种子；自动测试不能依赖不可重复的 `Math.random()`；
- 场景会话只存在内存中；刷新或重新进入 FLY 会恢复规范起点；
- 不自动 push、部署或引入在线运行服务。

## 9. 非目标

- 本阶段不增加第二种飞行器；
- 本阶段不实现固定翼、喷气式、旋翼、航天飞机或 UFO；
- 本阶段不实现多云、雷暴、台风、结冰、冰雹或全球实时天气；
- 本阶段不建立完整球形地球、洲际导航、轨道力学或太空飞行；
- 本阶段不实现多人、账号、保存进度、排行榜或网络地图；
- 本阶段不修改 SOURCE 的反应堆、实验室、轻水、玻璃、控制台或相机业务模型；
- 本阶段不承诺逐分子流体、全尺寸 CFD、逐根织物有限元或认证级飞行训练精度；
  使用的工程代理必须公开、量纲正确并可验证。

## 10. 验收

### 10.1 构建与静态检查

- `npm run build` 退出码为 0；
- `npm test` 和 `./scripts/run-validation.sh` 的全部已配置检查通过；
- 浏览器 console 没有影响运行的错误；
- 没有把图片或预渲染视频作为天空、云、地面或飞行器主要模型。

### 10.2 场景生命周期

- 打开网站先看到可操作的 SOURCE/FLY 三维选择，不自动启动任一业务场景；
- 连续执行 `选择 SOURCE → 返回 → 选择 FLY → 返回 → 再选 SOURCE`，任意时刻只有
  一个 RAF 主循环、一个活动音频会话和一个活动物理世界；
- SOURCE 在三个视口中的加载、首次交互、相机、AUTO/MANUAL 和玻璃行为不回退；
- 返回后上一场景不再产生声音、状态变化、监听器重复触发或 GPU 资源增长。

### 10.3 物理确定性

- 相同种子、初始状态和控制时间线在 30/60/120 Hz 渲染节奏下运行相同物理时长，
  高度、速度、燃料和内部温度结果保持在文档规定的数值容差内；
- 主燃烧器输入先改变燃料与热状态，再改变密度、净浮力和运动；
- vent 输入产生相反的热/浮力趋势；停止两者后系统连续演化，不瞬间冻结；
- 同一高度的不同世界水平位置可以采样不同局部风，跨高度能够采样不同风层；
- 无风或随风同速时相对风阻接近零；只改变世界速度而保持相对空气速度不变时，气动
  结果不应改变；
- 燃料只能减少；燃料耗尽后燃烧器不能继续产生热功率和火焰/声音。

### 10.4 可玩旅程

- 用户能够根据指南启动燃烧器，使篮筐从地面真实离地；
- 用户能够通过燃烧、等待冷却和 vent 控制上升、近似平飞与下降；
- 水平航迹由风场和不同高度风层形成，没有直接平移作弊；
- 程序化区块在持续航行中无明显裂缝、整世界跳动或重复累积的活动刚体；
- 至少发生一次浮动原点迁移，迁移前后相对位置、相机、速度、风采样和音频连续；
- 自动安全降落不瞬移，最终在合格地表接地并进入 `RECOVERED`；
- 燃烧器、风、织物/悬挂和落地声音能够追溯到实时状态。

### 10.5 浏览器视口

- 使用 Playwright MCP 检查 `390 × 844`、`768 × 1024`、`1440 × 900`；
- 每个视口画布非零、无页面溢出、场景选择可用、操作指南可读且能关闭；
- 桌面视口完整执行场景切换、出发、手动燃烧/放气、跨风层、浮动原点和自动降落；
- 移动视口至少能用触控完成对应物理控制，不依赖键盘才能出发或安全结束；
- 报告记录物理步长、种子、热气球质量清单、温度/浮力样本、燃料曲线、风层样本、
  原点迁移次数、自动降落轨迹、三视口 console 和未验证项。

## 11. 一手资料

- [FAA Balloon Flying Handbook FAA-H-8083-11B](https://www.faa.gov/regulations_policies/handbooks_manuals/aviation/Balloon_Flying_Handbook)：
  热气球结构、理论、气象、飞行操作、着陆与回收；
- [Cameron C-Type 官方规格](https://cameronballoons.com/c-type/)：C-100 包络几何、
  分片、体积、重量和材料特征；
- [Cameron Burners and Tanks](https://cameronballoons.com/burners-and-tanks/)：NEO 燃烧器、
  框架、阀门与燃料瓶产品结构；
- [NASA Glenn Lift Equation](https://www1.grc.nasa.gov/beginners-guide-to-aeronautics/lift-equation-2/)
  与 [Aerodynamic Forces](https://www1.grc.nasa.gov/beginners-guide-to-aeronautics/aerodynamic-forces/)：
  相对流动、动压、升力/阻力和压力积分基础；
- [NASA Air Properties](https://www1.grc.nasa.gov/beginners-guide-to-aeronautics/air-properties-definitions/)：
  标准空气状态和高度依赖；
- [NASA Technical Memorandum 2005-213659, U.S. Standard Atmosphere 1976 appendix](https://ntrs.nasa.gov/citations/20050207438)：
  分层温度、压力和密度公式；
- [FAA Aviation Weather Handbook FAA-H-8083-28B](https://www.faa.gov/regulationspolicies/handbooksmanuals/aviation/faa-h-8083-28b-aviation-weather-handbook)：
  航空气象总基线；
- [NOAA/NWS Turbulence](https://www.weather.gov/zme/safety_turb)：热气流、机械湍流和
  风切变；
- [NOAA/NWS Thunderstorm Life Cycle](https://www.weather.gov/spotterguide/life) 与
  [NOAA Hurricane FAQ](https://www.aoml.noaa.gov/hrd/tcfaq/F1.html)：后续雷暴和台风
  结构的正式起点。
