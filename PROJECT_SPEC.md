# 当前目标与验收规范

本文件是当前目标、范围和验收标准的唯一正式来源。

## 产品定义

- 网站只有一个全屏页面；
- 当前唯一场景正式命名为 `SOURCE`；
- 页面没有可见文字、标题、导航、链接或内容区；
- 蓝色是核心颜色；
- 页面主体是可交互的真实三维物理场景。

## 当前目标

以提交 `b69973f65e34d983489328193a6f363158c4eb62` 的 SOURCE 场景为暂定基线，只
重构观察相机的输入与运动手感。右键围绕画面中心锁定的焦点缓慢旋转，滚轮连续细致
推进，方向键平移画面中心；相机仍可进入水下、堆芯附近和地下设备区。

本阶段不改变实验室、反应堆、地下设备、轻水、切伦科夫、控制台或玻璃业务模型，也
不生成外部园区、地形、天空或云层。相机完成后必须停止，由所有者检阅当前实验室，
再为实验室改造和完整核科学园区建立新的正式任务。

创建本任务不表示所有者已经接受上一任务的实验室视觉质量，只表示现有场景成为本轮
相机改造的工程基线。详细执行边界和量化验收以 `.agent/next-task.md` 为准。

### 玻璃

- 每块玻璃是独立三维网格和独立刚体；
- 具有质量、惯性、重力、摩擦、阻尼、碰撞、休眠和稳定堆叠；
- 指针通过物理约束抓取和拖动，释放后由物理世界接管；
- 格栅玻璃和地板玻璃可抓取；墙壁、天花板玻璃属于固定建筑结构，不可抓取；
- 抓取时鼠标只控制水平位置，`W/S` 控制上下移动，`A/D` 只控制绕世界竖直轴的
  左右偏航；
- 抓取成功后停止已有随机角速度，锁定俯仰和翻滚；释放时保留位置与朝向但不注入
  随机角速度，后续真实碰撞仍可使玻璃旋转；
- 材质能辨认为有厚度的透明玻璃，不读成蓝色塑料或无形方块；
- 撞击、滑动和滚动声音由接触冲量、相对速度、角运动、接触点和持续接触状态驱动；
- 音频只在用户手势后启用，并限制触发频率、并发和峰值；
- 实心玻璃由反应堆池上部实体安全格栅支承，不漂浮在水上，也不使用隐形平面；
- 每块玻璃具有会话内持续的耐久和损伤状态；
- 损伤由有效碰撞质量、法向相对速度、冲量、撞击点、角运动和既有裂纹驱动；
- 玻璃按 `INTACT → MICRO_DAMAGED → CRACKED → FRACTURED` 演化；
- 真正破碎时主要碎片拥有独立三维几何、刚体、声音和水体交互，不能只显示裂纹贴图；
- 页面打开或刷新时，玻璃恢复规范初始位置、耐久 `1.0`，裂纹和碎片全部清除。
- 地板由独立动态玻璃砖铺成并由透明结构碰撞层承托；移开地板砖后可以观察地下设备；
- 墙壁和天花板由有厚度、砖缝、折射和静态碰撞的独立固定玻璃砖构成，不用贴图
  冒充砖块。

### 反应堆池系统与轻水

- 反应堆原型固定为 Pavia TRIGA Mark II，池内介质固定为轻水；
- Pavia 资料是堆芯尺寸、A–F 格位、装载和三根控制棒构型的主要依据；其他设施资料只能补充
  不冲突的通用部件或物理现象；
- 生物屏蔽、水箱、上部作业结构、桥架、格栅、控制棒驱动、连接杆、实验设施、
  仪器探头、管路、电缆和气动软管分别建模并具有真实连接；
- 支承格栅采用同型 TRIGA 正式资料支持的可放下安全格栅构型，并优先匹配 Pavia
  现场照片；同型资料不得冒充 Pavia 的精确尺寸；
- 至少建模中央套管、Rabbit 气动传输管、Lazy Susan 旋转载样架、热柱和当前镜头
  可见的水平实验通道；
- 冷却系统表达堆芯自然循环、一回路水箱水、闭式中间回路、三回路冷源和两台
  换热器的真实功能拓扑；
- 轻水必须是独立三维系统，具有可辨认的边界、深度、表面或体积光学状态；
- 轻水必须清澈到能从池口识别堆芯、控制棒、反射体和池底结构，不能由单一高不透明
  蓝色表面遮挡反应堆；
- 水面反射/折射、随光程变化的吸收/散射、水下雾化、热羽流折射和焦散使用同一
  水体/相机状态；
- 轻水具有静水、自然循环、简化热分层、水面传播、边界反射、阻尼、浮力、阻力、
  入水和局部棒运动响应；
- 水面事件由反应堆机构、水下部件、入水碎片或池体边界的物理数据触发，能量输入
  停止后必须衰减；
- 反应堆主要部分分别建模，不得合并为一张平面图案；
- 至少包括池体、堆芯支撑、燃料元件、控制机构、反射体以及可从当前视角表达的
  冷却或仪器结构；
- 主要部分拥有独立对象、材质和状态；
- 至少存在一条可解释运行链：控制机构状态变化影响功率代理，并进一步影响轻水、
  切伦科夫辉光或仪器状态；
- 切伦科夫位于堆芯附近水体，强度由稳态功率与独立脉冲功率通道驱动；250 kW
  默认取景必须明显可见，低功率不得常亮或把整池水均匀染蓝；
- 辉光由局部发光几何、体积散射、粒子视觉代理及受控 bloom/曝光共同表达。粒子
  只作为功率驱动的光传播代理，不拥有碰撞、浮力、损伤或声音；
- 结构参考公开可靠的真实研究堆资料；网页模型可以简化，但不得混合互相矛盾的
  反应堆结构。

### SOURCE 双模式连续运行

- 每次打开或刷新创建新会话，统一重置反应堆池、轻水、格栅、玻璃、碎片、损伤、
  音频和时钟；
- resize、设备旋转、标签页隐藏或 WebGL 恢复不得错误触发新会话；
- 页面加载后保持可见的联锁复位状态，并同时提供 `AUTO` 与 `MANUAL` 两种控制模式；
- 第一次在控制台热点以外进行有效指针、触控或键盘交互时，同时解锁音频、释放
  场景时钟并进入 `AUTO`；
- 第一次直接操作三维控制台热点时，同时解锁音频、释放场景时钟并进入 `MANUAL`，
  执行该次真实控制指令，不先偷偷启动自动程序；
- `AUTO` 按一条连续运行程序经历辅助设备就绪、低功率临界、一次 Pavia 历史脉冲、
  脉冲后传热、稳态功率提升和 250 kW 平衡；
- `MANUAL` 允许用户通过无文字三维控制台启动、SCRAM、控制冷却泵、连续提插三根
  控制棒、切换运行/脉冲工况并触发满足联锁条件的脉冲；
- 保留现有无文字 MANUAL 三维控制台，并在旁边增加物理独立的无文字 AUTO 控制台；
- AUTO 控制台提供启动、阶段、联锁、棒位、功率和冷却反馈；MANUAL 与 AUTO
  控制台必须调用同一 session controller、命令、联锁和物理状态；
- 自动程序必须调用与人工控制相同的命令、联锁、动力学和热工状态，不能使用另一套
  预录动画或直接写最终数值；
- 自动运行期间执行任一人工控制指令会原位切换到 `MANUAL`，停止自动调度并继承
  当前棒位、功率、温度、流量、水体、结构和玻璃状态；
- 从 `MANUAL` 重新进入完整 `AUTO` 只能在 SCRAM/停堆安全状态开始，不能在任意
  功率状态重放自动序列；
- 任意时刻只有一个控制权所有者；AUTO 调度与 MANUAL 指令不得同时驱动同一设备；
- 上述阶段只是可测试状态，不是互不相干的动画；棒位、功率、温度、轻水、冷却、
  结构、声音和玻璃必须继承前序结果；
- 脉冲从资料支持的低功率条件触发，由 TRANS 气动动作、毫秒功率峰和 UZrH 负温度
  反馈构成；不能从 250 kW 稳态直接触发，也不能表现为核爆；
- 脉冲期间燃料向水的传热近似可忽略；水体热响应在脉冲后通过燃料传热和自然循环
  发展，不能让整个池水瞬间爆炸式跳起；
- TRANS 机构反力经桥架和格栅向玻璃传递有界振动；玻璃的位移、声音和损伤仍由
  格栅接触、摩擦、质量和冲量决定，不得在 `PULSE` 时直接改速度或扣耐久；
- 运行声音来自已建模的控制棒驱动、电磁/气动机构、辅助系统、水体和结构，
  不把核反应本身合成为无来源的科幻声音；
- 浏览器禁止首次访问自动播放声音时，不得绕过权限；
- 详细连续运行和反应堆池部件以 `docs/engineering/REACTOR_POOL_SYSTEM.md` 为准；
  场景耦合、玻璃损伤和锁定决定以 `docs/engineering/SOURCE_SCENE.md` 为准；
  实验室、地下设备、相机、水体光学、辉光粒子、玻璃建筑和双控制台以
  `docs/engineering/SOURCE_LAB_OPTICS.md` 为准。

### 实验室、地下设备与自由观察

- 实验室形成完整三维空间，包括池边作业层、维护通道、吊装、通风、照明、仪器柜、
  管线、电缆和安全设施，不再是围绕反应堆的简单空盒子；
- 地下层至少建模三回路/两换热器相关设备、净化支路、泵阀、集水/排水、TRANS
  气动、电气管廊和样品气动传输管段；
- 设备功能拓扑依据 Pavia 和不冲突的正式 TRIGA 资料；没有公开竣工图的地下位置
  明确标记为实时代理，不冒充 Pavia 精确布局；
- 右键按下时从画面中心选择场景命中点并锁定为本次旋转焦点；无命中时使用稳定的
  视线回退焦点，拖动期间不得漂移或跳换；
- 右键旋转目标灵敏度约为 `0.0018 rad/像素`，滚轮经过 `deltaMode` 归一化、
  单事件限幅和无过冲平滑后连续推进；
- 上下左右方向键沿当前画面 `up/right` 平移相机和焦点，松开或失焦后立即停止；
- `Home/F` 恢复规范初始取景；抓取玻璃期间相机输入暂停；
- 相机可以穿过水面、进入堆芯附近、地板以下和地下设备层，不参与刚体碰撞，也不
  推动设备或玻璃；
- 相机跨越水面时连续切换水上/水下光学；相机移动、缩放、穿层均不得重置会话或
  改变 AUTO/MANUAL 控制权；
- 抓取期间 `W/S/A/D` 完全归玻璃控制；未抓取时基础相机导航使用右键、滚轮与
  方向键，不再让 `W/S/A/D/Q/E` 同时承担相机身份。

## 建模约束

- 不使用照片、背景图、预渲染画面或二维贴片代替主要物体；
- 不使用整面 shader 绘制虚假的三维结构；
- shader 只用于真实几何上的材质、照明、折射、反射、水体或辉光；
- 纹理只用于表面细节，不能代替主要几何；
- 运动不能是与系统状态无关的装饰性循环；
- 不用屏幕抖动代替真实结构或轻水的动力学响应；
- 不用隐形碰撞平面解释实心玻璃位于池口上方；
- 透明结构碰撞层只允许承托所有者明确要求的动态玻璃地板，不得替代反应堆格栅、
  建筑设备或其他可见承重结构；
- 当前页面不增加任何可见文字。

## 工程约束

- 保持纯前端静态构建；
- 保持 Vite、Three.js、cannon-es 和 Web Audio 技术路线；
- 不增加后端、数据库、账号、运行时密钥或在线服务依赖；
- 不自动 push 或部署；
- 刚体、水体和 SOURCE 连续运行控制器使用明确时间步，毫秒脉冲使用独立高频或
  解析代理；渲染帧率不能显著改变运行顺序、脉冲输入和损伤结果；
- 会话状态只存在内存中，不持久化玻璃位置或耐久；
- `prefers-reduced-motion: reduce` 下停止非必要持续动画，并保留可用静态画面。

## 非目标

- 其他场景、页面、滚动内容或内容区；
- 个人介绍、标题、导航、说明文字、链接或联系方式；
- 与当前物理场景无关的功能和远期设想。
- 本阶段不扩建或重做实验室，不生成外部园区、道路、建筑、地形、天空、云层或天气；
- 本阶段不提前确定所有者检阅后才会提出的实验室改进内容。

## 验收

### 构建

- `npm run build` 退出码为 0；
- `./scripts/run-validation.sh` 如实报告所有已配置和未配置检查；
- 构建产物不包含已删除页面的代码或资源。

### 页面

- DOM 中只有承载物理场景所需的结构，没有可见文字和其他内容区；
- 页面固定为一个视口，无纵向或横向滚动；
- 不加载图片背景或平面反应堆兜底；
- 浏览器 console 没有影响运行的错误。

### 物理场景

- 玻璃可抓取、拖动、释放、碰撞和稳定堆叠；
- 抓取时没有随机旋转；鼠标水平移动、`W/S` 上下移动、`A/D` 只做水平偏航，
  俯仰与翻滚保持锁定；
- 地板动态玻璃砖可以逐块移动并显露地下设备；墙顶玻璃砖保持固定；
- 玻璃由有可见厚度和连接的实体格栅支承，格栅与桥架/反应堆池结构相连；
- 静止物体不持续抖动、漂浮、穿透或产生声音风暴；
- 玻璃耐久与损伤由可复现碰撞数据驱动，破碎后原完整刚体不再参与物理；
- 打开和刷新会恢复玻璃初始位置与完整耐久，resize 和标签页切换不会复位；
- 从生物屏蔽、池体、桥架、格栅、驱动、仪器、实验设施、冷却设备和连接管线确认
  反应堆池是完整三维系统；
- 从大厅、地面设备、地下管廊、泵阀、换热/净化/气动/电气设备确认实验室不是
  简单背景；
- 轻水位于格栅和堆芯之间，并具有独立可见的物理/光学表现；
- 默认水上视角能透过轻水看清反应堆；水下视角具有连续的体积衰减、散射和焦散；
- 250 kW 平衡时堆芯附近蓝色辉光清楚可见，脉冲辉光短促有界，低功率不常亮；
- 轻水波动、浮力、阻力和声音由物理事件驱动并正确衰减；
- 控制机构变化会驱动至少一个可观察的系统响应；
- 控制台外首次交互启动一次 AUTO 连续程序；控制台首次交互进入 MANUAL；
- AUTO 完整到达历史脉冲和最终平衡，MANUAL 能独立完成启动、升功率、脉冲和 SCRAM；
- AUTO→MANUAL 接管不复位物理状态，MANUAL→AUTO 受停堆安全条件约束；
- 脉冲在毫秒时间尺度发生，机构振动、局部水动力和脉冲后传热按不同时间尺度传播；
- 规范玻璃布局通常不会被脉冲直接击碎；不稳定或已有损伤的玻璃可以因真实二次
  碰撞而开裂或破碎；
- 声音触发与真实物理数据有可检查的映射。
- MANUAL 控制台能力完整保留，AUTO 控制台可独立发起并反馈同一自动程序，两者
  不建立重复状态或同时驱动设备；
- 自由相机可到达水下、地下和设备背面，移动过程中不推动刚体、不复位物理状态。

### 视口

- 使用 Playwright MCP 检查 `390 × 844`、`768 × 1024`、`1440 × 900`；
- 每个视口画布非零、无溢出、场景可交互；
- 检查会话重置、两种首次交互分流、AUTO 完整运行、MANUAL 控制链、人工接管、
  历史脉冲、脉冲后水体/玻璃响应、最终平衡、双控制台、自由相机、水面跨越、
  地下设备、地板/墙顶玻璃、约束抓取、切伦科夫粒子、玻璃损伤/破碎和音频解锁；
- 不使用 Bash Playwright 脚本代替 MCP 验证。
