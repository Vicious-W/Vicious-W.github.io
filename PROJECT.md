# 项目文档：个人主页（Personal Homepage）

> `PROJECT_SPEC.md` 现在是项目目标、范围与验收标准的唯一正式来源。
> 本文件继续作为项目的**技术记忆与追踪记录**：说明关键决策、当前进度和历史演变，不能覆盖 `PROJECT_SPEC.md`。
> Claude Code 在实现前应同时阅读两者，并在实质进展后持续更新本文件第 5 节「当前状态」和第 7 节「决策与目标演变日志」。

---

## 1. 这是什么
- **项目**：我的个人主页（个人网站）。
- **类型**：静态网站（纯前端，无后端、无数据库）。
- **背景**：这是仓库所有者的**第一个开发项目**。仓库由一个 GitHub 仓库拉取而来，已有初步开发。

## 2. 目标与愿景
- 做一个**具有强烈表现力 / 视觉冲击力 / 交互有趣**的个人主页。
- 表现力优先：动画、富交互、（可能的）3D 等前端效果是重点。
- 整体气质 / 用途方向：整体的颜色以蓝色为主；网站应用展现个人的个性与艺术追求，并具有有趣的交互。
- 第四屏水面场景的长期目标：追求**真实、自然、具有物理感**的夏日雨后小水塘。画面应呈现雷阵雨后蓝而透彻的天空、具有体积感的积雨云、水面对蓝天白云的自然倒影、可透过水面隐约看到的水草、小鱼，以及鼠标划过水面时产生的涟漪互动。总体目标不是简单装饰性背景，而是一个有真实水面质感、深度层次和生命感的实时视觉场景。

## 3. 工作约定（给 agent 和协作者——本节最重要）
- **所有者不做网页开发，也不打算为这个项目学习网页开发。** 网页开发不在所有者的学习范围内。
- 因此：**实现细节全权交给 Claude Code。** 所有者负责设定目标、判断最终的观感与效果；Claude Code 是唯一业务代码实现者，负责技术选型和实现。
- Codex 的常规角色是**独立审查者**：依据 `PROJECT_SPEC.md`、Git diff、实现报告与验证证据检查结果，只写正式审查报告，不直接修改业务代码。完整边界见 `AGENTS.md`、`CLAUDE.md` 和 `REVIEW_CONTRACT.md`。
- 两个 Agent 不同时修改业务代码，以明确 Git 提交和 `.agent/` 内的正式报告交接，不依赖临时聊天记录。
- 在「静态网站」这个前提下，尽量把效果做得惊艳。
- 可以大胆改动——本项目由 git 管理，任何一步都能回滚。
- **Claude Code 每次有实质进展或做出决策后更新本文件**（第 5 节「当前状态」＋第 7 节日志），让后续实现与审查不必重新摸索。

## 4. 技术决策

**已确定**
- 静态网站，无后端。
- 托管平台：GitHub Pages
- 设计方向 / 整体气质：颜色以蓝色为主；简约、高级、交互有趣；展现个性，具有几何、数学、自然美感。
- 第四页水塘场景应升级为独立的高表现力实时图形模块。现有原生 WebGL shader 可以保留为原型参考，但长期目标需要更强的场景组织、材质、后处理、交互和资源管理能力。
- 第四页开发技术栈确定为 **Vite + Three.js + 原生 GLSL shader**：
  - **Vite**：负责开发服务器、模块化、依赖管理、资源打包与缓存控制；构建产物仍是静态文件，可部署到 GitHub Pages。
  - **Three.js**：负责第四页实时场景的场景图、相机、渲染器、纹理/模型资源、后处理与交互管线。
  - **GLSL shader**：负责实时水面法线、折射、局部反射、高光、涟漪高度场和纹理鱼合成；植物、底床和静态天空倒影由原创生态底图提供。
  - **WebGL2 优先**：作为表现力与兼容性的基线。WebGPU 暂不作为主路线，以避免兼容性和工程复杂度过早上升。
- 第四页生态表现改为**原创写实底图 + 实时水膜的混合渲染**：植物、底床、遮挡和局部天空倒影由专门生成并筛选的横版/竖版生态底图提供；Three.js / GLSL 负责水面折射、微波、高光、涟漪和纹理鱼。停止在单个 fragment shader 中逐像素程序化生成整套植物、鱼和云层。

**待定（开放问题）**
- 是否在当前混合方案上继续增加独立表面浮叶层、反射/深度控制图和更复杂的鱼群行为；这些增强只有在当前整体真实感方向得到确认后再决定。

## 4.1 第四页开发

> 本节专门记录第四页（Contact / 水塘场景）的需求、问题边界与实现方案。凡是关于真实水面、蓝天白云倒影、水下水草、小鱼、鼠标涟漪和实时图形性能的讨论，都归入“第四页开发”。

**第四页的目标**
- 将当前第四页从“带动态倒影的背景”升级为“真实夏日雨后小水塘实时场景”。
- 画面预期：雷阵雨后，水塘倒影蓝天白云；天空蓝而透彻；云是具有体积感的积雨云；水面有真实波纹、微小扰动、高光、反射与折射；透过水面可隐约看到水下随水摆动的水草；偶尔有小鱼游过；鼠标划过水面时产生自然涟漪。
- 交互预期：鼠标/触控经过水面时影响水波，涟漪继续传播、衰减并影响倒影和水下折射。
- 真实感优先级高于短期实现速度；允许分阶段重建，不要求一次完成全部目标。

**当前第四页存在的问题**
- 旧版单个 fragment shader 同时程序化生成云、植物和鱼，既产生明显重复图案，也造成过高 GPU 成本；该路线已于 2026-07-17 被写实混合方案取代。
- 当前版本已具备独立的原创横竖生态底图与横竖天空反射纹理；反射率按观察距离/角度变化，生态底图经过水柱吸收、深度散射和折射后形成水下层，并配合动态积云、焦散、持续细波、叶片滴水环、矩形物理波场、纹理鱼和鼠标/触控涟漪。
- 后续问题主要是继续按所有者观感精调反射/透射比例、涟漪强度和鱼的存在感；不再增加会重新遮满水面的浮叶层。

**第四页分阶段方案（按当前混合路线）**
1. 工程升级：迁移到 Vite，安装并引入 Three.js，保持现有四屏页面结构和 GitHub Pages 静态部署能力。
2. 写实生态底层：为桌面和移动分别制作原创生态底图，让水草、底床、遮挡、深度和局部天空倒影先在静态画面中成立。
3. 实时水膜：用轻量 shader 处理动态法线、折射、局部反射、高光和矩形涟漪波场，不再在每个像素内生成整套生态。
4. 生命层：使用透明纹理鱼在水下缓慢游动并响应局部波高；不增加会重新遮满水面的独立浮叶层。
5. 物理感与交互统一：让鼠标/触控涟漪同时影响局部折射与明暗边，并让鱼对指针产生克制避让。
6. 性能与降级：按设备限制 DPR、波场短边和帧率；接近第四页时再动态加载 Three.js，不可见时停止渲染，失败时显示静态生态底图。
7. 验收：用 Playwright MCP 在桌面和移动视口实际渲染，确认 canvas 非空、构图正确、圆形涟漪有效、文字不重叠、console 无报错。

**第四页验收标准**
- 第一眼能明确识别为雨后小水塘，而不是抽象蓝色纹理。
- 水面同时具有倒影、透明感、波纹、高光与轻微扰动。
- 天空和云看起来自然，云有体积感和层次，倒影中的云不会像贴图硬盖在水面上。
- 水下水草和小鱼可被察觉，但不过度抢戏；它们应被水体颜色、折射和波纹自然调和。
- 鼠标划过水面时涟漪自然传播并逐渐衰减，能真实影响画面。
- 页面仍是静态站，部署到 GitHub Pages 后不依赖后端。

## 5. 当前状态
> 仓库已有初步开发
- 已完成（2026-07-22，协作可观测性/额度控制）：Claude 第 2 轮因月度额度中断后留下的两个未完成文件已
  可恢复地保存为具名 Git stash `recovery: interrupted Claude implementation round 2 at usage limit`，
  工作区回到最后一份有效审查检查点。`.agent/runtime.env` 现在显式固定项目级模型策略，不再继承个人会话中
  昂贵的 Opus/max 选择：Claude 使用最新 `sonnet` + `high`，Codex 使用 `gpt-5.6-sol` + `high`；启动前
  预检会校验模型名和 effort。父循环每次退出（PASS、三轮上限、额度/权限/验证错误等）都会自动生成
  `.agent/artifacts/cycle/latest-summary.md`，按轮概括 Claude 的主要改动、Codex 的判定与问题标题、提交、
  中断原因和下一步；`./scripts/agent-cycle.sh summary` 可随时重新生成并直接查看。Codex 原始日志也改为
  `codex-round-N.log`，不再被下一轮覆盖。简报固定使用中文界面；后续 Codex 在保留契约机器标记和英文章节
  名的同时，正文与问题标题使用简体中文。
- 已完成（自动闭环第 1 轮，2026-07-22）：第一屏两条主线首轮实现。① 玻璃层重调质量/重力/摩擦/恢复/
  阻尼/solver/睡眠与有限力拖拽约束，手感更重、堆叠更稳；材质去塑料感（去 clearcoat、放长体吸收、去饱和
  料色）；新增 `src/glassAudio.js`，用 cannon-es 的法向冲击速度、切向滑动速度和接触点位置驱动 Web Audio
  撞击/滑动空间声音，仅首次手势后解锁并带阈值/节流/voice 上限/限幅。② 新增 `src/reactorModel.js`，依据
  TRIGA Mark II 研究堆公开资料建立真三维反应堆（池体/甲板、石墨反射体、栅格板、同心圆环燃料棒 InstancedMesh、
  4 根控制棒+驱动桥、冷却/仪器导管、功率指示条、切伦科夫辉光），各系统独立对象/材质/状态，并有「控制棒
  位置→功率代理→辉光/指示条」可解释运行链。旧 `reactorShader.js`/`reactorScene.js` 池面降级为首屏瞬开与
  Three.js 失败兜底。Playwright MCP 三视口实测：拖拽/堆叠、音频手势闸门、运行链联动、构图与 console 0 error
  均通过；详见第 7 节最新日志与 `.agent/implementation-report.md`。等待 Codex 审查。
- 进行中：所有者已确定第一屏下一阶段的两条主线，并写入 `.agent/next-task.md`：① 继续重做玻璃立方体的重量感、稳定性、真实材质，并增加由实际碰撞/滑动状态驱动的空间声音；② 推翻当前像贴图的 shader 反应堆，先依据公开可靠资料选定与俯视水池构图相符的研究堆原型，再把池体、堆芯支撑、燃料组件、控制机构、冷却/仪器结构等分别建成具有独立状态和可信联动的 Three.js 三维系统。旧 reactor shader 只保留为瞬开与失败降级。
- 当前阻塞：修复 TTY 预检后，完整父循环已真实显示并进入 `Claude implementation round 1/3`；Claude/Playwright MCP 进程正常启动，但模型调用最终返回账户 `monthly spend limit`，约 6 分钟后退出 1。父脚本按设计没有提交、没有启动 Codex、没有增加轮次并清理锁；需所有者在 Claude 用量设置中解除额度限制后重新运行 cycle。
- 已修复：首次在真实 TTY 启动 `agent-cycle.sh cycle` 时，预检的 `timeout claude auth status` 把子进程置于独立后台进程组；Claude CLI 访问终端后进入 `T` 停止态，而普通 TERM 超时无法唤醒，导致永远看不到实现轮次标题。预检现使用前台 timeout、`/dev/null` stdin 和 5 秒强杀宽限；正式 Agent 也断开 stdin。循环锁新增 PID + `/proc` 启动时间身份，可自动回收硬中断遗留锁而不误判活动流程。
- 已完成：双 Agent 无人值守权限层加固。Claude 从本机 `bypassPermissions` 收紧为项目级 `dontAsk`，只允许项目内业务读写、必要 npm/只读 Git/本地预览、公开检索和 Playwright MCP，未预批能力直接拒绝，Git 暂存/提交仍只归中立包装器；Codex 固定为只读且 `approval_policy="never"`。新增不启动 Agent 的 `agent-cycle.sh preflight`、专用 npm/MCP 缓存、30 秒心跳、Claude 2 小时/Codex 1 小时超时、精确进程组终止、权限/认证/MCP/超时分类以及 Claude 受保护文件机械拦截。任何异常都停在当前阶段，不增加轮次、不启动下一个 Agent。
- 已配置：所有者进一步明确双 Agent 的期望是自动串行闭环，而不是手动依次启动。新增 `run-implementation.sh` 和 `agent-cycle.sh implement|cycle`：从干净 Git 基线自动调用一次 Claude Code、验证并本地提交，再调用只读 Codex 审查、归档并提交报告；`CHANGES_REQUIRED` 自动进入下一轮，PASS 或最多三轮后停止。全流程不并发、不 push/deploy/reset/clean/rebase/switch，不在失败后擅自丢弃工作区。
- 已完成：根据《项目双 Agent 协作体系改造报告》完成一次性协作基础设施改造。`PROJECT_SPEC.md` 成为目标/范围/验收的唯一正式来源；Claude Code 被明确为唯一业务代码实现者，Codex 默认转为只读审查者；新增 `REVIEW_CONTRACT.md`、`.agent/` 正式交接目录及统一验证、只读审查、有限命令分发脚本。当前自动检查只有依赖完整性与生产构建；测试、lint、类型检查、CI 均如实标记为未配置，网页验证继续使用 Playwright MCP。
- 文件结构：单页 `index.html` + `style.css`；前端入口为 `src/main.js`；第一页为 `src/reactorScene.js`（重水池，原生 WebGL1，首屏瞬开/兜底）+ `src/glassCubes.js`（玻璃立方体层，Three.js + cannon-es，延迟加载）+ `src/reactorShader.js`（两者共用的池面 GLSL）；第四页水塘场景为 `src/pondScene.js`；第四页原创资源位于 `src/assets/pond/`，包含横版/竖版生态底图、横版/竖版蓝天积云反射纹理与透明鱼素材；构建配置为 `vite.config.js`，依赖记录在 `package.json` / `package-lock.json`。旧 `scene.js` 暂留为历史参考，页面不再加载。配套资源 `texture-carpet-titanium.webp`、`texture-sketch-paper.webp`、`texture-forest-floor.webp`。仓库根目录另有若干已提交但未引用的早期实验资源。
- 已完成：首页已重构为**「极简开发者主页」**——单页四屏滚动，主题色依次为 纸白 → 红 → 绿 → 蓝；衬线字体 + 蓝色点缀；前三页以 CSS/纹理和入口脚本动画为主，第四页由 Three.js/GLSL 实时水塘场景负责。四个区块：Hero（Vicious-W）/ About / Selected work（VKC_Study、c-learning）/ Contact（邮箱 + GitHub）。各区块为滚动触发渐显。
- 历史阶段曾修复：旧 `scene.js` 水面反射统一使用 WebGL shader 生成动态蓝天白云，并处理缓存不一致；该脚本现已退出运行链路，只保留作历史参考。
- 已补内联 SVG favicon（data URI，纸白底衬线「W」），消除 `favicon.ico` 404；用 Playwright 逐屏渲染核对，四屏正常、console 0 报错。
- 历史阶段已完成：项目迁移到 Vite + Three.js + GLSL，第一版 `pondScene` 曾使用程序化蓝天、植物和鱼；其中工程骨架与涟漪机制保留，程序化生态表现已被新版替换。
- 已完成：第四页水塘方向纠偏。根据 `水塘参考/` 中的参考图，第四页先暂时弱化天空倒影，优先重做“清澈俯视浅水生态”本体：密集底草、茎叶植物、羽毛状水草、浮萍/大浮叶、小白花/气泡感、小鱼点缀和透明水膜。
- 已完成：根据所有者对旧第四页“完全未达到预期”的反馈，第四页完成第二次路线级重建。旧版程序化叶片、浮叶、鱼和 FBM 云层已删除，改用原创写实横版/竖版生态底图、透明纹理鱼与轻量实时水膜；桌面文字区移到左侧深水负空间，移动端使用独立竖构图。
- 已完成：保留并优化涟漪物理交互；桌面/移动的矩形波场短边分别为 160 / 112 像素并以 30 Hz 模拟，波场随视口比例保持圆形传播；同时限制 DPR、离屏停止渲染，并加入静态底图、WebGL 创建失败和 context lost 降级。
- 已完成：根据所有者希望“植物退到水下、扩大动态蓝天白云倒影”的反馈，第四页改为天空主导的反射/透射分层。新增原创横竖蓝天积云纹理，shader 从纹理中分离云体并使其缓慢漂移；水下生态与天空反射不再写死为单一混合比例。
- 已完成：鼠标/触控拖尾波场同步驱动云层倒影的较强反射位移、水下层的较弱反向折射和水面明暗边；三条纹理鱼会读取局部波高与指针距离，转向、加速摆尾并向扰动外侧避让。移动端保留低密度鱼与低分辨率波场。
- 已完成：根据“没有水感、没有纵深、水草看不见、倒影像贴图”的反馈重建光学混合。水面反射率现在从近景约 27% 逐渐提高到远景最高约 76%；近景重新透出清晰水草，深水区根据底图亮度增加水柱颜色和散射模糊。底图中原有的低色度白云亮斑会被识别并压暗，避免提高透水率后出现两套云影。
- 已完成：倒影加入水色染色、低对比度云体、粗糙度模糊和细波位移；水下层加入随深度衰减的动态焦散，表面亮纹由波面曲率产生。另加入三处低频叶片滴水环作为雨后环境动态，它们与鼠标波场一样真实改变法线、倒影和明暗，而不是叠加二维圆环。
- 进行中：**全站蓝色主题重构**已启动——前三页都将重做，第四页观感精调暂停（成果保留）。第一页文字全部临时隐藏（`style.css` 末尾注释标明的临时 visibility 块，HTML 原样保留），旧 Cantor 图随纸白版设计一并隐藏。
- 已完成：第一页背景艺术第一版——**「透过玻璃砖俯视核反应堆重水池」**（所有者构想：底层反应堆 / 中层重水 / 表层无色透明玻璃砖，切伦科夫蓝色辉光贯穿画面；参考图为仓库根目录 `蓝色重水1/2`、`玻璃块1/2/3`，仅用于说明概念）。实现为 `src/reactorScene.js`：原生 WebGL2 单通道 shader（首屏即时渲染，不依赖 Three.js），画面含外圈平台、带螺栓法兰环、被辉光照亮的井壁、方形燃料组件格阵、辉光呼吸、旋转光柱、上漂闪烁微粒；重水层有整体微晃，玻璃砖层有细密菱形压纹折射、斜面边框色散、接缝、砖间透光差异与斜向反光。含 CSS 径向渐变静态兜底、reduced-motion 静帧、离屏/隐藏暂停、context lost 恢复与 DPR 限制。
- 已完成：根据所有者反馈"玻璃砖块非常假"，专项重写玻璃层折射/色散/反光模型，详见第 7 节日志。
- 已完成：根据所有者第二轮反馈（"还是不够真""每块砖应该不一样/物理特征随机"），给每块玻璃砖引入四个独立随机数，弧度、起弯半径、凹槽深浅、压花纹理方向/频率、亮度冷暖、霾面/清澈、气泡瑕疵、接缝深浅均按砖独立随机；同时把单层柔光高光换成"锐利小亮点+柔和大亮斑"双层高光，并加静态表面微粒感。详见第 7 节日志。
- 已完成：根据所有者反馈"反应堆细节不够、像动画片，蓝色辉光很假"，联网查证真实切伦科夫辉光/燃料组件外观后重写池底与辉光模型（棒状燃料元件子网格取代棋盘格平板、辉光更集中于棒尖、加入快速闪烁颗粒感），详见第 7 节日志。
- 已完成：根据所有者要求取消第一页原有的鼠标视差效果——移除 `uPar` uniform、指针位置监听与三层差异视差坐标偏移，法兰/井壁/池底三层现在共用同一份静止坐标，画面不再随鼠标移动而偏移。
- **已推翻（2026-07-22）**：以上所有关于"玻璃砖"的工作都是在**一整块屏幕空间 shader 上分格**做的。所有者指出方向从根上就不对——在一整块画面上"划分"格子，永远得不到一个个真实的物体。玻璃层已重做为 `src/glassCubes.js` 里一个个独立的三维立方体（Three.js 网格 + cannon-es 刚体 + 鼠标拖动堆叠），详见第 7 节最新一条日志。下面这几条保留作为历史记录。
- 已完成（已废弃，见下一条）：第一次尝试给玻璃层加透视，用的是"在砖块边缘涂一条阴影+高光带、宽度按半径手调"的二维贴片做法。
- 已完成：根据所有者反馈"仍看不出每个玻璃方块是正方体"，玻璃层改为**真实的光线-立方体求交**：立方体本体在 shader 内有确切的三维范围（顶面 z=0、厚度=边长），光线穿过顶面后按折射斜率下行，先撞侧壁还是先到底面由几何解算决定，两者的分界线就是底面棱边的投影——于是每块砖内部会出现一圈朝画面中心偏移的方框，偏移量随该砖离画面中心的距离增大。详见第 7 节日志。
- 已完成：根据所有者"不要深色槽壁、玻璃正方体直接互相贴合"的要求，去掉砖缝与槽壁，改用**侧壁全内反射**模型（玻璃临界角约 41.8°，近垂直下行光线打在竖直侧壁上必然全反射，侧壁本就该是镜子而非暗槽）；同时修正了"顶面菲涅尔反射混向亮蓝"的错误（本场景光源在玻璃下方，上方接近全黑，边缘应压暗而非反白）。详见第 7 节日志。
- 已完成：第一页表层重做为**一个个独立的、有物理体积和物理特性的三维玻璃立方体**，鼠标可以抓起任意一块、拖到任意位置、叠在别的立方体上（`src/glassCubes.js`：Three.js `MeshPhysicalMaterial` 真折射 + `cannon-es` 刚体 + 点对点约束拖拽）。底层重水池仍由 `src/reactorScene.js` 负责首屏瞬开与兜底，两者共用 `src/reactorShader.js` 的同一份 GLSL。
- 下一步：自动 Agent 闭环执行 `.agent/next-task.md` 中的玻璃物理/材质/声音与真实三维反应堆重建；当前阶段不处理文字回归和第二、三、四屏重构。

## 6. 环境与工具链
- 系统：Windows + WSL2（Ubuntu）。
- 编辑器：VS Code（在 WSL 终端用 `code .` 启动）。
- 版本控制：git + GitHub（所有者的每个项目都会建立仓库并用 git 管理）。
- 在用的 agent：Codex、Claude Code。
- 参与者日常命令速查：`PROJECT_COMMAND_MANUAL.md`。
- 可迁移的多 Agent 协作方法论：`AI_Project_Meta_Method_v2.0_2026-07-23.md`。
- 安装依赖：`npm install`
- 本地开发预览：`npm run dev -- --port 8000`（若 8000 被占用，Vite 会自动尝试下一个端口）
- 生产构建：`npm run build`
- 统一自动验证：`./scripts/run-validation.sh`
- Codex 只读正式审查（需干净工作区和明确提交）：`./scripts/run-review.sh`
- 自动实现—验收闭环：`./scripts/agent-cycle.sh cycle`（串行，最多三轮）
- 权限/环境预检：`./scripts/agent-cycle.sh preflight`（不启动 Agent）
- 分步与状态入口：`./scripts/agent-cycle.sh preflight|implement|validate|review|status|archive`
- 多轮简报：`./scripts/agent-cycle.sh summary`（最新文件：
  `.agent/artifacts/cycle/latest-summary.md`；每次生成同时保留本地时间戳历史）
- 未配置：自动测试、lint、类型检查、CI、仓库内 Playwright 测试套件；页面验收使用环境中的 Playwright MCP。

## 7. 决策与目标演变日志（追加式，最新在最上面）
> 每当目标发生变化、做出一项技术决策、或完成一个阶段性开发，就在此追加一条，并标注日期。
> 这一节是这个项目的「记忆」——它记录我们是怎么一步步走到现在的。

### 2026-07-23 — 项目指令手册与多 Agent 元方法 v2.0
- 新增 `PROJECT_COMMAND_MANUAL.md`，把本地预览、预检、完整循环、分步入口、模型/effort、轮数、报告位置和
  常见故障恢复整理为面向所有者与参与者的命令速查；内容以实际脚本行为为准，不代替正式规范。
- 在完整复盘旧版 `AI_Project_Meta_Method_v1.0_2026-07-10.txt` 和本项目自动化实践后，新增
  `AI_Project_Meta_Method_v2.0_2026-07-23.md`。新方法保留“项目高于 Agent、显式记忆”的地基，把多 Agent
  的核心升级为角色/权限/证据分离、单一写入者、中立父控制面和有上限的串行状态机。

### 2026-07-22 — 项目级模型/effort 固定 + 自动多轮简报 + 中断恢复
- 背景：首次真实闭环证明 Claude→Codex 串行交接成立，但个人 Claude Code 会话此前选用 Opus 4.8/max，
  很快耗尽月度额度；第 2 轮在写到一半时中断，留下能构建但运行时接线未完成的
  `src/glassCubes.js`/`style.css`。这两个文件已用具名 Git stash 完整保存后从工作区移走，未提交为有效实现，
  当前 HEAD 仍是 Codex 第 1 轮审查检查点 `7555e53`。
- 模型策略：不再静默继承全局选择。`.agent/runtime.env` 新增 `CLAUDE_MODEL=sonnet`、
  `CLAUDE_EFFORT=high`、`CODEX_MODEL=gpt-5.6-sol`、`CODEX_REASONING_EFFORT=high`；实现/审查脚本显式传给
  两个 CLI，预检限制 model 为安全 slug、effort 为 `low|medium|high|xhigh|max`。选择理由是本项目已有独立
  审查兜底，Sonnet/high 能覆盖复杂 Three.js/物理/浏览器工具工作，同时避免 Opus/max 的额度消耗；只有自动
  三轮后仍不能解决的问题才由所有者决定临时升级。
- 可观测性：新增 `scripts/generate-cycle-summary.sh` 和 `agent-cycle.sh summary`。简报从本轮 Git 检查点中的
  `.agent/implementation-report.md` 与 `.agent/latest-review.md` 提取每轮主要改动、验证、问题等级/标题和
  详情命令；额度/权限等中断也会记录未形成检查点的轮次。`cycle` 用 EXIT trap 在所有终止路径生成最新简报
  和时间戳本地历史，不改变业务 Git 状态。
- 细节保留：Claude 日志本来已按 `claude-round-N.log` 分轮；Codex 的 `prompt.md`/`codex.log` 也改为
  `prompt-round-N.md`/`codex-round-N.log`，避免后续审查覆盖前一轮原始记录。简报固定为中文界面，审查
  prompt 要求 Codex 保留契约的固定 section/VERDICT 标记、但用简体中文撰写正文和问题标题。

### 2026-07-22 — 第一屏重建第 1 轮：真三维 TRIGA 反应堆 + 玻璃物理/材质/声音
- 目标：执行 `.agent/next-task.md` 的两条主线首轮实现。
- **Workstream B（反应堆真三维重建）**：查证公开资料后选定 **TRIGA Mark II 型开放式水池研究堆**作为
  原型——它的构图正是「从水池正上方俯视堆芯」，且资料充分。来源：General Atomics TRIGA 产品页；
  TRIGA Mark II 稳态表征 arxiv 1503.00873（堆芯直径 44.6 cm、上下栅格板间距 64.8 cm、91 个 A–G 环位置）；
  Oregon State 1.1 MW TRIGA Mark II；U.S. DOE 切伦科夫辐射说明。来自资料的结构：燃料元件按同心圆环
  A(中心)–F 排布（6n 规则 1/6/12/18/24/30 共 91 位）、圆柱形燃料元件、上下铝栅格板、4 根控制棒在 D 环、
  石墨反射体环、开放水池、切伦科夫辉光集中于棒尖偏蓝白。刻意抽象（不声称工程精度）：环位置用 6n 近似、
  栅格板不做逐孔布尔运算、尺度归一到玻璃立方体边长、辉光用加性发光盘+点光源+燃料自发光近似。
- 新增 `src/reactorModel.js`：把池体内衬/池底/井唇、环形甲板、石墨反射体内外壁+顶环、上下栅格板、
  同心圆环燃料棒（InstancedMesh×3 亮度档，约 87 根）+ 棒顶端接头、4 根控制棒+连接杆+导向套管+跨池驱动桥、
  2 根辐照/束流导管+沿壁冷却回路、12 段功率指示条、切伦科夫辉光盘+泛蓝晕+核心蓝色点光源分别建成独立
  场景对象。运行链：控制棒缓慢正弦抽出/插入（周期约 60 s、机械迟滞）→ 功率代理带迟滞跟随 + 通量闪烁 →
  燃料自发光/辉光/点光源/控制棒竖直位置/指示条段数同步响应。旧 shader 池面降级为瞬开+失败兜底。
- **Workstream A（玻璃物理/材质/声音）**：`src/glassCubes.js` 底层由「贴 shader 的平面」换成真三维反应堆，
  玻璃 `transmission` 现在折射真实几何（视差/遮挡/独立运动可确认是立体）。物理重调：重力 −20、质量 1.5、
  摩擦 0.45、恢复 0.03、接触刚度 1e7/松弛 3、solver 迭代 14、阻尼 0.12/0.35、睡眠收紧、step 子步 4；拖拽用
  有限力(55)点对点约束，抬得起但有重量滞后，拖动期间提高角阻尼防乱转。材质去 clearcoat、放长体吸收、
  去饱和料色，避免染成有色塑料。新增 `src/glassAudio.js`：撞击声=带通噪声瞬态+高频正弦分音，滑动声=常驻
  噪声 voice 随切向速度，声像随接触点位置；仅首次手势解锁，带最小冲击阈值/全局节流/voice 上限/限幅/离屏挂起。
  接线：刚体 `collide` 读法向冲击速度触发撞击，每帧扫 `world.contacts` 取最大切向相对速度驱动滑动。
- 验证（Playwright MCP，dev 8000）：1440×900 见同心圆环棒阵+辉光+反射体，透过玻璃可见折射的真实堆芯；
  合成拖拽抓起静止块(y=0.5)提升至 y≈1.74（有重量滞后）、拖到堆芯上方松手歪斜堆叠、原位空出；音频手势前
  `unlocked=false`、pointerdown 后 `true`；9 s 采样 `rodInsertion` 0.554→0.539 单调、`power` 跟随微升，
  运行链成立；768×1024 与 390×844 竖构图无横向溢出。三视口 console 0 error（仅截图触发的 ReadPixels
  性能 warning）。`npm run build` 退 0，重型代码仍在延迟 chunk，首屏瞬开不受影响。证据截图见
  `.agent/artifacts/round1-*.png`。
- 待定（留给所有者判断）：外圈玻璃在暗甲板下折射偏「深蓝金属块」，清澈感主要在堆芯附近；驱动桥正俯视下
  仍是对称十字（已做细做暗）；`transmission` 不采样透明物体，玻璃透玻璃/辉光盘不进折射，叠高后上块看到堆芯。

### 2026-07-22 — 首次越过预检启动 Claude；账户月度额度安全熔断
- TTY 修复提交后再次运行完整父循环，正式看到 `=== Claude implementation round 1/3 ===` 和 Claude PID/7200 秒监督信息，证明父脚本到实现者的启动链路已经打通；Claude 进程与 Playwright MCP 均处于正常运行态而非 job-control 停止态。
- Claude 最终明确返回 `You've hit your monthly spend limit` 并退出 1。包装器保持工作区干净，`CURRENT_ROUND=0`、任务 `READY`、Codex 未启动、锁已清理，因此没有把账户额度失败误算为实现或审查轮次。
- 运行库新增 `USAGE_OR_BILLING_LIMIT` 分类，覆盖月度用量、额度、余额、计费和速率上限；烟雾测试新增相应假进程。新版 Claude 同时提示 `Write(path)` 不是有效文件权限规则、`Edit(path)` 已覆盖全部文件编辑工具，因此从项目/本机/CLI 三处移除冗余 `Write` 规则，保留项目内 `Edit` 与 `.git` Edit deny。

### 2026-07-22 — 修复真实 TTY 预检永久停止与硬中断陈旧锁
- 所有者报告手动运行 `./scripts/agent-cycle.sh cycle` 后没有出现 Claude 实现轮次标题。宿主进程检查复现并定位：流程停在 `timeout 45 claude auth status`，Claude 状态为 `T`（job-control stopped）；`timeout` 默认创建的新进程组与真实终端交互冲突，且只有 TERM、没有 KILL 宽限，所以停止态子进程和父预检均永久等待。
- 修复：认证/MCP 私密检查改用 `timeout --foreground --kill-after=5s` 并把 stdin 接到 `/dev/null`；正式 Claude/Codex 子进程也断开 stdin，所有提示均通过位置参数传入。修复后的完整 TTY 预检约 4.5 秒通过，不再停在认证检查。
- 原有锁只是空目录，父进程被强杀后会永久阻挡后续启动。共享运行库现为锁写入 PID、`/proc/<pid>/stat` 启动时钟、命令和 UTC 时间；获取锁时同时核对 PID 与启动时钟，活动锁继续拒绝并发，死亡/PID 复用/旧版无元数据锁则仅清理四个已知文件并原子重建。烟雾测试覆盖了陈旧锁回收和活动锁拒绝。
- 本次问题发生在预检阶段，Claude 实现与 Codex 审查均未启动，`CURRENT_ROUND` 仍为 0，业务代码和任务状态没有变化。

### 2026-07-22 — 无人值守权限层：最小授权、启动前预检、心跳与故障熔断
- 权限原则确定为“项目内够用、项目外不放权”，而不是给 Agent 整机全权。Claude 的本机 `bypassPermissions` 已移除，跟踪的 `.claude/settings.json` 与本机覆盖均使用无人值守 `dontAsk`；allowlist 只覆盖项目内业务读写、实现所需 npm、只读 Git、检索、本地预览和 Playwright MCP，denylist 明确阻止 Git 控制命令、sudo、环境变量转储、另起 Agent、破坏性删除和常见凭据目录读取。未预批能力直接拒绝，不等待用户回答。Codex 审查继续由 CLI 强制 `read-only` + `approval_policy="never"`。
- 新增 `agent-preflight.sh`/`agent-cycle.sh preflight`：在父循环开始及每个对应 Agent 真正启动前检查工作区、轮次状态、脚本语法、权限 JSON、必需 denylist、禁用模式/危险 allowlist、依赖、Git 身份及写入能力、两端登录和 Playwright MCP。预检本身不启动 Agent；任一关键项失败时 cycle 返回 6。
- 新增共享 `scripts/lib/agent-runtime.sh` 和 `.agent/runtime.env`。子进程每 30 秒报告心跳；Claude/Codex 默认单轮上限分别为 7200/3600 秒，超时或外层收到中断时会按已知 PID 的独立进程组 TERM→KILL，不用模糊进程名。`npx` 使用专用 Playwright 缓存，避免为默认 `~/.npm` 放宽主目录权限。
- Claude 结束后、验证与暂存前新增控制面检查：角色/规范、权限配置、任务/轮次/审查状态、审查历史和 Agent/验证脚本均不可由业务实现轮修改，同时核对本地 Git 配置、refs 和预暂存状态；自动提交关闭仓库 hooks 与 GPG 签名提示。失败按权限、认证、MCP/浏览器、超时或普通子进程错误分类，写入本地 `last-stop.env`，不增加轮次也不接着启动 Codex。
- 验证：权限 JSON 可解析，所有 Agent shell 脚本通过 `bash -n`，`git diff --check` 通过；完整外部预检在“允许当前基础设施改造处于脏工作区、跳过当前 Codex 沙箱内的 `.git` 写探针”诊断模式下全部通过，包括 Claude/Codex 登录与 Playwright MCP。`test-agent-runtime.sh` 使用假的 shell 子进程验证 SUCCESS、PERMISSION 和 TIMEOUT 三条路径，未启动真实 Claude/Codex。本轮没有修改网页外观或行为，因此不需要 Playwright 页面回归。

### 2026-07-22 — 自动闭环启用；下一阶段重建玻璃物理/声音与真实三维反应堆
- 所有者试玩后确认三个玻璃问题：手感偏飘、玻璃质感仍不真实、缺少随物理运动产生的声音。下一轮需要系统调整质量/惯性/阻尼/摩擦/恢复/约束/solver/休眠，重做厚度与折射/吸收/粗糙度/接触光学，并用 Web Audio 将碰撞冲量、相对速度、接触位置及滑动状态映射为受节流和限幅保护的空间声音。
- 所有者同时推翻第三层当前路线：现有反应堆像一张贴图。正式主体必须先从官方/研究机构等可靠公开资料选择与当前俯视池式构图一致的研究堆原型，再把池体、内衬、堆芯支撑/栅格、燃料组件、控制机构、冷却/循环/仪器可见结构分别建模；各主要系统有独立对象、材质、状态和更新逻辑，并至少形成“控制状态 → 功率代理 → 水体/切伦科夫辉光/仪器响应”的可解释联动。旧 shader 降级为瞬开/失败兜底。
- 验收边界：“真实运行”是静态网页中结构、尺度层次、机械、材料、光学和状态耦合可信，不声称达到工程级中子输运、热工水力或安全分析精度；将旧 shader 贴在三维平面、合并成单体模型或播放互不相关的循环动画均不通过。
- 协作方式纠偏：所有者明确希望 Claude 实现与 Codex 验收自动一个接一个运行。新增 `.agent/next-task.md` 和 `run-implementation.sh`，`agent-cycle.sh cycle` 现在从干净基线自动串行执行 Claude → 验证 → 本地实现提交 → 只读 Codex → 审查归档/提交；CHANGES_REQUIRED 自动修正，PASS/三轮上限/工具或安全异常立即停止。禁止并发、push、部署、reset、clean、rebase、切分支和无限重试。
- 基础安全测试：所有脚本 `bash -n` 与 `git diff --check` 通过；在当前尚未建立基线提交的脏工作区试运行 `agent-cycle.sh cycle`，按设计在调用 Claude 前返回 2，并保留全部改动与所有者文件。
- 首次自动启动诊断：Claude CLI 使用“无位置参数、从 stdin 读取长提示”的调用在本机持续空转并最终只返回 `Execution error`，期间没有任何仓库改动，循环按设计以退出码 5 停止且清理锁。最小探针确认 Claude 非交互本身正常后，入口改为以 `--` 后的位置参数显式传入提示；同一套 allow/disallow 工具边界探针在约 4 秒内返回 `TOOLING_READY`。失败尝试未增加轮次，也没有产生实现提交。

### 2026-07-22 — 建立“Claude Code 实现 / Codex 独立审查”的双 Agent 协作体系
- 所有者新增《项目双 Agent 协作体系改造报告》，明确长期角色：Claude Code 是唯一业务代码实现者，Codex 是独立结果审查者；二者不同步修改，通过仓库文件和 Git 检查点交接。
- 文档职责重新划分：新增 `PROJECT_SPEC.md` 作为目标、范围、非目标和验收标准的唯一正式来源；`PROJECT.md` 保留为技术决策、当前进度和历史记忆；`AGENTS.md`、`CLAUDE.md` 不再互相导入同一套规则，而是分别约束审查者与实现者；新增 `REVIEW_CONTRACT.md` 固定 Blocker/Major/Minor/Suggestion、证据字段和 PASS/CHANGES_REQUIRED 判定。
- 新增 `.agent/` 交接面：`latest-review.md`、`implementation-report.md`、`state.env`、只追加 `review-history/` 和被忽略的本地 `artifacts/`。初始文件明确标记尚未执行实现/审查，没有伪造 PASS。
- 新增三个脚本：`run-validation.sh` 自动识别锁文件与 package scripts，当前实际执行依赖检查和 Vite build，并把测试/lint/类型检查标为 `NOT CONFIGURED`；`run-review.sh` 要求完全干净的工作区与当前 HEAD，先验证再用 `codex exec --sandbox read-only` 发起恰好一次审查，校验报告格式后才更新最新报告与归档；`agent-cycle.sh` 只提供 validate/review/status/archive，不提供自动实现或无限 cycle。
- 安全边界：正式审查在工作区不干净时退出码为 2 并保留所有文件；最多三轮，PASS 后的新提交自动开始新轮次，失败修复提交则延续计数；不自动 reset/clean/checkout/rebase/push/deploy，不调用 Claude 非交互实现，不覆盖旧审查历史。
- 验证：三个脚本 `bash -n` 语法检查和 `git diff --check` 通过；入口权限为可执行；统一验证实际运行通过依赖完整性检查和 `npm run build`，测试/lint/类型检查准确显示 `NOT CONFIGURED`，浏览器视觉验证显示 `MANUAL REQUIRED`；在当前包含本轮改造和所有者未跟踪素材的工作区运行 `run-review.sh`，脚本按设计拒绝启动并返回 2，证明脏工作区保护有效。本轮没有修改页面外观或行为，故无需 Playwright 视觉回归。

### 2026-07-22 — 路线级推翻：玻璃层改为**一个个真实的三维立方体**（可鼠标拖动堆叠）
- 所有者指出方向从一开始就错了：**"你是在一整块玻璃上『划分』出一块块玻璃"**。屏幕空间分格不管怎么调折射、倒角、压花，都不可能真实，因为画面里根本不存在一个个物体。要的是**互相靠在一起的、独立的玻璃正方体**。
- 同时明确了第一页鼠标的用途：**可以抓起任意一个玻璃立方体，拖到任意位置放下，包括叠在别的立方体上面**。所以表层是"一堆摆在桌面上的玻璃方块"，每个都独立、有物理体积和物理特性。
- 工程重构：
  - 新增 `src/reactorShader.js`——池子本身的 GLSL 单一来源，被原生 WebGL 兜底版和 Three.js 池面材质共用，避免玻璃层接管时画面跳色。
  - `src/reactorScene.js` 删掉整个屏幕空间玻璃层，只保留重水池，退为**首屏瞬开的即时画面 + Three.js 不可用时的兜底**；新增 `suspend()` 供玻璃层接管后停掉重绘。
  - 新增 `src/glassCubes.js`：Three.js 场景。每个立方体是一个 `RoundedBoxGeometry` 网格 + 一个 `cannon-es` Box 刚体（质量、碰撞、重力、摩擦、休眠）。折射用 `MeshPhysicalMaterial` 的 `transmission` 真实计算。
  - 拖拽用 `PointToPointConstraint` 把立方体吊在一个不参与碰撞的"手"上，而不是直接设坐标——这样立方体会在手上摆动、并能推开挡路的邻居，松手后恢复重力落下压在别的立方体上。
  - 新增依赖 `cannon-es`。
- **首屏没有变重**：Three.js（514 kB）与玻璃立方体+物理（94 kB）都是延迟加载的独立 chunk，入口 chunk 仍只有 14.5 kB / gzip 6.5 kB。加载失败则停在原生 WebGL 池面上。
- 调参过程（Playwright 实拍驱动）：① 初版 FOV 42° 相机离得太近，画面边缘的方块几乎整个侧面都露出来，读成一堆乱翻的盒子；压到 22°（长焦俯视）后重新读成"俯视的一层"，同时仍看得出是正方体。② 初版让 60 块砖从很高的随机位置乱落，而可见范围恰好只装得下这么多块，结果互相砸中卡死，堆成一团；改成每块正对自己那格上方、落差很小、按离画面中心的距离错开时间后，才铺成整齐的一层。③ 体吸收的衰减距离给到 2.4 时方块被染成实心蓝、像有色塑料，放宽到 7.0 并去掉 `clearcoat`（清漆层是塑料/车漆的特征，玻璃没有）后才透。
- 验证：`npm run build` 通过；Playwright 实拍 1440×900 与 390×844，立方体铺成一层、透视方向正确、透过它们能看到被折射的燃料格阵；合成指针事件实测拖拽——原位置空出、被拖的那块最终架在层面之上并微微歪着，确认抓起/移动/落下/堆叠整条链路有效；console 0 error / 0 warning。
- **已知限制**：Three.js 的 `transmission` 只采样不透明物体那一遍渲染，所以**玻璃透过玻璃看不到后面那块玻璃**。单层平铺时几乎看不出来；叠高后上面那块会直接看到池底而不是下面那块。要根治需要自己写光线追踪，是另一个量级的工程。
- 待定：所有者试玩拖拽手感（砖块大小、数量、拖动时的提升高度、松手后的下落感）；外圈暗处的方块目前偏"深蓝金属块"，是否需要让它们更透。

### 2026-07-22 — 去掉槽壁：立方体直接贴合 + 侧壁全内反射（**已被上一条整体推翻**）
- 所有者反馈上一版"就像一个个玻璃正方体放在深色的槽里，槽壁是不透明的"，要求去掉深色槽壁、让玻璃正方体一个挨一个直接贴合，并进一步提升真实度。
- 关键物理认识：玻璃 n=1.5 的**临界角约 41.8°**，而侧壁的法线是水平的、光线又接近垂直下行，相对壁法线的入射角远超临界角——所以贴合立方体的竖直侧壁**本来就不是"暗槽壁"，而是全内反射镜面**。每块立方体相当于一节光导：光线在内部折返后才从底面射出，底面出射位置是直线位置关于侧壁的**镜像折叠**结果，而折叠的那条折痕正是底面棱边的投影。这既满足了"去掉槽壁"，也比暗槽更真实。
- 实现：砖缝常量删除（`hCube` 直接取 0.5，立方体填满整格）；用三角波把未折叠出射位置 `u = d + slopeIn·T` 镜像折回 `[-hCube, hCube]`，折叠位移量转成场景采样偏移。底面棱边改为"细亮线 + 外侧一圈微暗晕"（单纯一条亮线像贴上去的描边）。相邻立方体接触处只保留一条极细的界线（内侧细高光 + 正中暗线），不做成槽。顶面枕状高度场的幅度大幅调低（正方体顶面基本是平的，只保留少量铸造不平整度）。
- 一并修正的两个错误：① **色散被施加在了整个偏移量上**（包括全反射折叠项），而全内反射并不分光——这会在折痕处炸出假的绿色/彩色条纹；改为把折射偏移与折叠偏移分开，只对折射项做三次采样色散。② **顶面菲涅尔反射混向亮蓝** `(0.55,0.78,0.98)`——但本场景的光源在玻璃**下方**，上方几乎全黑，顶面反射进来的应是暗环境；这个错误会在每块砖边缘糊出一圈发白的宽带（这正是"看起来像磨砂条"的来源）。改为反射一个很暗的冷色环境后，边缘变成压暗而非反白。同时把柔光大亮斑压到原来的 1/3（顶面变平后，宽高光会糊成一片）。
- 调参过程（Playwright 实拍驱动）：初版把底面棱边亮线加到峰值 0.30 的加性亮度，在暗背景上炸成贯穿画面的宽亮带；把线收窄、亮度降到约 1/4 并配合外侧暗晕后成立。全反射折叠增益初设 0.85，最远处像位移达 0.78 个砖长，把辉光圆环切成明显的方块状台阶；降到 0.50 后既保留折返感又不撕裂画面。
- 验证：Playwright MCP 实拍 1440×900 与 390×844。立方体之间已无深色槽，直接贴合；每块内部仍可见朝画面中心偏移的底面方框，且偏移量随离画面中心的距离增大；console 0 error。
- 待定：所有者判断贴合后的立体感是否足够（可调 `FOLD_GAIN` 折返强度、底面棱线亮度、`CAM_HEIGHT` 透视强度）。

### 2026-07-22 — 玻璃层改为真实光线-立方体求交（这一版才读得出正方体）
- 所有者反馈上一版透视"效果不太行，仍然看不出每个玻璃方块都是正方体"。
- 原因（上一版方向就错了）：上一版的"侧面"本质是**二维贴片**——在每块砖边缘涂一条阴影+高光带，宽度靠半径手工调。它背后没有任何三维几何，所以再怎么调也只是"边缘有条纹"，永远不会读成立方体。
- 关键几何认识：相机架在画面正中央上方俯视时，**顶面与光轴垂直，其网格在屏幕上本来就该是均匀方格**，这部分不需要"制造"透视（上一版误以为要让网格收敛）。真正产生立体感的是竖直侧面：光线穿过顶面进入玻璃后会一边下行一边向画面外偏，若在抵达底面之前先撞上立方体朝外那面侧壁，该像素看到的就是侧壁内侧；而"撞侧壁"与"到底面"两区域的**分界线正好是这块立方体底面棱边的投影位置**。玻璃透明，本来就该透过顶面看到自己的底面棱边，且离画面中心越远偏移越大——这才是"看得出是正方体"的几何依据。
- 实现：shader 内为玻璃层建立确切三维范围（顶面 z=0，厚度 = 边长，即真正的正方体），相机高度 `CAM_HEIGHT` 以砖块边长为单位（7.5）。每像素由屏幕位置解出顶面命中点与下行斜率，折射按小角度斯涅尔近似把斜率压缩 1/n，再解析求出撞各轴侧壁所需深度 `dzSide`；`dzSide < 厚度` 的区域即侧壁内侧，`dzSide/厚度` 给出沿壁的深度用于着色，接近 1 处画出底面棱边亮线。砖缝改为有深度的槽：视线越正越像直视深井（更暗），越斜越早看到对面立方体侧壁外侧（更浅更亮）。原枕状高度场的局部坐标同步归一化到顶面范围内，不再延伸进砖缝。
- 调参过程（Playwright 实拍驱动）：初版砖缝 0.035、侧壁压暗到 0.42 且场景采样偏移 0.42 个砖长，结果读成"凹进去的窗格"而非玻璃方块，且侧壁在明暗交界处出现过饱和的靛蓝色块（偏移过大导致侧壁一下采到远处强辉光，再叠蓝色附加项）。将砖缝收到 0.020、侧壁提亮到 0.62/0.90、采样偏移收到 0.24、削弱蓝色附加与去饱和偏色后，立方体结构清晰但不再喧宾夺主。另外让砖缝按所在处的场景亮度透一点光（`+ sc * 0.18`），避免亮区中央出现一张死黑网格。
- 验证：Playwright MCP 实拍 1440×900 与 390×844。画面中心那块砖的内框几乎与外框同心（接近垂直视线），越靠画面边缘内框偏移越大、侧壁带越宽，符合透视几何；console 0 error。
- 待定：所有者判断透视强度是否合适（`CAM_HEIGHT` 调小=透视更强）、砖缝宽度与侧壁明暗是否需要再调。

### 2026-07-22 — 玻璃层加入透视的第一次尝试（二维贴片，已被上一条取代）
- 所有者精准指出玻璃层"假"的根本原因：整个视角像是垂直笔直俯视每一块砖的顶面，没有透视——理论上这层如果是玻璃正方体，应该能看到方块的侧面结构，且不同方块随着离视角中心的距离不同，透视程度也该不同。
- 原因：此前的玻璃高度场（`glassHeight`）只模拟了砖块顶面的枕状弧面，整套折射/色散/菲涅尔都基于"正上方垂直看向顶面"的假设，画面里所有砖块无论在屏幕什么位置都用同一套垂直视角处理，天然没有位置相关的透视差异。
- 实现：在 `main()` 里新增一套与顶面高度场并行的透视几何——以画面中心为隐含摄像机的正下方，每个像素按其相对屏幕中心的半径 `radial` 和方向 `radialDir` 计算：半径越大，越能看到砖块朝向画面中心一侧的竖直棱面（`sideBand`，按 4 个坐标轴方向的朝向权重加权，对角位置的砖会同时露出两侧棱面）。棱面渲染为"先压出一圈阴影、贴近真正棱边处再叠一道随砖随机强度的高光"，与顶面原有的折射渲染在最终各自计算完后按 `sideBand` 混合。
- 调参过程（Playwright 实拍驱动）：初版棱面颜色给到高饱和度接近纯白，效果虽在正确的位置出现，但因为同一列/行的砖朝向相近，各自的窄边高光首尾相连，视觉上糊成一整根通栏亮条，反而显得假；调暗调窄后又几乎完全看不出来（乘法压暗在本来就暗的外圈区域没有可见变化）。用临时高饱和度洋红色排查后发现更深层的问题：贴着砖缝的像素本身强度只取决于方向角度、和半径无关，导致与画面中心同行/同列的砖不论远近都会全力显示，画出一圈方形轮廓而不是随半径平滑增强；补上一个随半径增长的整体强度系数后，方形轮廓消失，效果改为从画面中心向外平滑增强。最终改用"混合到绝对暗色（而非乘暗）压出阴影 + 贴棱边一圈随机强度高光"的方案，避免了通栏亮条也避免了在暗区不可见。
- 验证：本地 `npm run dev` 配合 Playwright MCP，用临时诊断色确认了透视强度按半径平滑增长（横版视口左右两侧最强、竖版视口上下两侧最强，符合宽高比对宽视角摄像机的几何预期）；换回正式配色后在 1440×900 与 390×844 视口截图确认画面中心砖块基本保持原样、外围砖块能看到朝内一侧的棱边阴影+高光，不再是纯粹的垂直俯视贴图；console 0 error。
- 待定：所有者查看透视强弱是否合适，是否需要继续加强/减弱棱面的宽度或亮度。

### 2026-07-22 — 取消第一页鼠标视差
- 所有者要求取消第一页随鼠标移动的动态效果（法兰/井壁/池底三层此前会按深度做不同幅度的位移视差）。
- 实现：删除 `uPar` uniform 及其在 shader 内对 `pFl`/`pW`/`pB` 三层坐标的偏移；JS 侧移除 `pointer` 状态、`pointermove`/`pointerleave` 监听与对应的 `dispose()` 清理、`draw()` 里的位置平滑插值。三层现在都用同一份静止坐标，鼠标不再影响画面。
- 验证：Playwright 将指针分别移到画布左上角与右下角截图对比，法兰环/核心位置完全一致，无偏移；console 0 error。

### 2026-07-22 — 反应堆与辉光重写：联网查证 + 棒状燃料元件网格
- 所有者反馈反应堆本体细节不够、"像动画片不像真的反应堆"，蓝色辉光也很假；要求联网查真实核反应堆图片/视频再做。
- 查证要点（WebSearch，见来源）：切伦科夫辉光强度与波长平方成反比，短波长（蓝/紫）远强于长波长，肉眼观感常被描述为"偏蓝白/带紫"而非纯青色；辉光来自大量独立带电粒子事件，真实影像有细碎闪烁的颗粒感，不是匀速起伏的一整片柔光；真实压水堆燃料组件是方形棒阵（常见按每束若干乘若干根排列），栅格板将一个个独立棒束固定成阵列，从上方看是"许多根独立发光棒"而非"一块带高光的平板"；池顶还有跟随体/控制棒组件与栅格板结构。
- 原因：旧版池底把每个"燃料组件"渲染成一整块平板——一次性 cbloom 高光 + 棋盘格交替亮度 + 单个深色格代表控制棒位，整体匀速呼吸（慢正弦）。这种"一格一色块"的表现和查证到的"一束束独立棒"外观差距明显，是"像动画片"的主要原因；辉光的匀速呼吸也和真实辉光的颗粒闪烁感不符。
- 重写方案：`scene()` 的池底部分改为两层网格——外层仍是旋转方形组件阵（数量从 11×11 减到 8×8，单个组件更大更清晰），组件内部新增 3×3 独立棒状元件子网格，每根棒有独立随机的燃耗/亮度/半径；少数组件格位整体替换为十字形控制棒/跟随体导向叶片，而非单纯压暗一格。辉光部分：组件边界外的 `heat` 项衰减更快（更集中于棒阵附近，弱化大范围匀速柔光的呼吸幅度），新增基于像素+时间双重哈希的"闪烁"项——只在已经发光的区域里，让稀疏像素以约 16Hz 频率快速明灭，模拟大量独立粒子事件的颗粒感；辉光主色 `hot` 从偏青白微调为偏中性蓝白，贴近查证到的"蓝白偏紫"描述。
- 调参过程（Playwright 实拍驱动）：初版子网格用 4×4、组件数仍是 11×11，导致单个组件里堆了 16 个独立高光源，总辉光面积/亮度比旧版单一光斑大得多，核心区域直接糊成一片死白，且过密的高频圆点和玻璃层折射叠加放大出明显红绿色边；把组件数降到 8×8、子网格降到 3×3、单棒辉光强度从 0.30～0.85 收到 0.08～0.24 后，核心重新呈现出可辨认的"发光棒阵"颗粒质感而不再过曝，色边也大幅减轻。
- 验证：本地 `npm run dev` 配合 Playwright MCP，在 1440×900 与 390×844 视口下确认池底呈现清晰的棒状元件网格而非棋盘格平板，部分格位可辨认出更暗的十字形控制棒结构，核心区域不再死白；console 0 error。
- 待定：所有者查看效果后反馈是否已经"够真"，或需要继续加强/调整棒阵密度、闪烁频率、辉光集中程度，之后再推进重水层、玻璃层微调或文字回归方案。

### 2026-07-22 — 玻璃砖块第二轮：每砖独立随机物理参数 + 双层高光
- 所有者第二轮反馈：新玻璃仍"不够真"，且每块砖看起来完全一样，要求物理特征按砖随机而非统一。
- 原因：上一轮的 `glassHeight()` 虽已用高度场取代人工位移，但穹顶弧度、起弯半径、凹槽深浅这些形状参数对所有砖块是同一组常量，只有细纹理相位按砖变化——从整体轮廓上看每块砖仍是同一块的复制粘贴；材质上也只有单一柔光高光，观感偏"哑光涂层"而非玻璃。
- 实现：`glassHeight()` 改为接收每砖四个独立哈希值（`hv`），穹顶弯曲量、起弯半径、凹槽深浅、压花纹理方向/频率/振幅、气泡瑕疵的有无与位置全部各自独立随机；约六分之一的砖块带一处偏心气泡瑕疵。`main()` 中每块砖的透光亮度范围加宽，约五分之一的砖块被判定为"霾面"（磨砂感，颜色去饱和、镜面高光按同一霾面系数减弱——频闪的漫射材质不该有清晰的镜面亮点），接缝暗线深浅也按砖随机，不再是统一网格线。高光从单一柔光换成"锐利小亮点（高次方，模拟点光源反射）+ 柔和大亮斑"两层叠加，并加入基于像素坐标的静态微粒感，避免曲面过于数学般光滑。
- 调参过程（Playwright 实拍驱动）：霾面砖叠加原本的双层高光与菲涅尔反白后，个别砖块（尤其叠加在反应堆最亮区域上方时）被压光曲线推到接近死白，读起来像渲染错误而非磨砂质感；把霾面砖的镜面高光整体按霾面系数打折、收窄透光亮度随机范围、降低霾面去饱和的整体增益后，同一位置仍明显偏亮但保留细节，不再是一块死白。
- 验证：本地 `npm run dev` 配合 Playwright MCP，在 1440×900 与 390×844 视口下确认相邻砖块的弯曲程度、亮度、清澈/霾面观感均可分辨为彼此不同，未再出现死白色块；console 0 error。
- 待定：所有者查看后决定是否满意当前玻璃质感，或需要继续加强/减弱某些参数，之后再推进重水层、反应堆本体或文字回归方案。

### 2026-07-22 — 玻璃砖块专项优化：真实高度场驱动的折射/色散/菲涅尔
- 所有者反馈第一页玻璃层"非常假"，要求专门优化玻璃砖块本体，使其有厚度和真实物理特征。
- 原因：旧实现只在整块砖表面叠加固定频率的正弦压花噪声做位移，真正的折射/色散仅在最外侧一圈（`rimAmt`）手工叠加，砖块中心到边缘之间没有连续坡度变化，观感更接近"贴图纹理"而非有体积的实体玻璃。
- 重写方案：新增 `glassHeight()` 高度场函数，为每块砖建模"中心近平台 + 外圈枕状弧面 + 贴框缝浅凹槽"的真实剖面，叠加带每砖随机相位的低频压花纹理；用有限差分对该高度场求梯度得到局部法线，折射偏移、棱镜色散强度、Schlick 菲涅尔反射比例全部由这一份坡度统一驱动，而非几组互相独立的手工参数。新增基于该法线的 Blinn-Phong 直射高光，以及贴近框缝处的轻微色差吸收（模拟穿过更厚玻璃路径变长）。
- 调参过程（Playwright 实拍驱动）：初版折射强度和压花频率设得过高，画面被过度扭曲成"融化"般的漩涡、色散夸张成彩虹边，反而比旧版更假；将折射系数、法线陡峭度、压花频率和振幅整体下调 3～5 倍，改为"中心几乎不弯折、只在靠近砖缝的外圈明显弯折"的剖面后，反应堆内容重新清晰可辨，同时每块砖呈现连续的枕状透镜畸变。随后小幅提高穹顶弯曲量、加宽并加强高光，并加入菲涅尔"反白"边缘光，让厚度感更明显。
- 验证：本地 `npm run dev` 配合 Playwright MCP，在 1440×900 与 390×844 视口下确认法兰环虚线、螺栓、燃料组件格阵在穿过每块砖时呈现连续弯曲而非贴图状菱形纹理；console 0 error。
- 待定：所有者查看效果后反馈，决定是否进一步加强穹顶弯曲/边缘反白强度，或转向优化第二层（重水层）/第三层（反应堆本体）。

### 2026-07-20 — 第一页第一版：玻璃砖下的核反应堆重水池
- 所有者给出第一页明确构想：三层结构——底层核反应堆、中层重水、表层无色透明玻璃砖，透过玻璃俯视池底，全画面被切伦科夫蓝色辉光浸透；每层应有各自的物理特征。参考图（概念说明用）：`蓝色重水1/2.png`、`玻璃块1/2/3`。
- 技术选型：第一页在首屏，必须即刻渲染，故不用按需加载的 Three.js，而是新写 `src/reactorScene.js` —— 原生 WebGL 全屏三角形单通道 fragment shader，三层在 shader 内合成：场景层（平台/法兰环+螺栓/井壁/燃料组件格阵）按深度取不同视差系数，重水层提供整体微晃、辉光呼吸、旋转光柱与上漂微粒，玻璃层在像素空间划分砖格，做细密菱形压纹折射、斜面边框强折射+色散、接缝暗线、砖间透光差异、斜向反光与大反光带，最后软拐点压光+抖动去色带。
- 调参过程（Playwright 实拍驱动）：初版玻璃压花波长过大，把法兰环揉成「花边餐垫」、螺栓成花瓣——压纹频率 6→13 并叠 29Hz 细纹、幅度减半后消除；井壁曾过暗形成突兀「黑甜甜圈」，提亮为被下方辉光照亮的渐变并加环向导轨圈；池底辉光衰减放缓，消除组件阵四周的生硬暗瓣。
- 工程细节：CSS 径向渐变作 shader 就绪前/WebGL 不可用时的静态兜底（`.reactor-ready` 后淡入）；reduced-motion 只画静帧、不挂 pointer 监听；IntersectionObserver 离屏暂停 + visibilitychange 暂停；context lost/restored 处理；DPR 桌面限 1.6 / 移动限 1.8；玻璃砖尺寸随视口自适应（92–230 CSS px）。
- 验证：Playwright MCP 实拍 1440×900 与 390×844，构图在横竖屏都成立；间隔 2s 两帧截图字节不同，确认动画运行；当前加载 console 0 error（历史记录中的 `g1` 报错是中途一次 HMR 旧状态，ReadPixels warning 为截图操作引发的浏览器提示）。
- **兼容性修正**：所有者在 Windows 浏览器实际访问时只看到 CSS 兜底渐变——初版用了 WebGL2 attributeless 绘制（`gl_VertexID`），该写法在 Windows ANGLE/D3D11 路径上不可靠，而无头 SwiftShader 软件渲染测不出来。已整体降级为最保守的 WebGL1 + 标准顶点缓冲 + GLSL ES 1.00（`experimental-webgl` 兜底、highp 带 mediump 回退、移除 `powerPreference` 请求），画面内容不变。**教训：本项目 shader 一律按 WebGL1 基线写，除非确实需要 WebGL2 特性；无头验证通过不代表所有者机器通过。**

### 2026-07-19 — 启动全站蓝色主题重构，第一页文字先行隐藏
- 所有者决定将整个网站按蓝色主题重新构建：前三页都要重构，第四页暂停精调（成果保留）。重构从第一页开始，方针是**背景艺术优先**——先把每页的背景艺术做好，文字怎样出现等重构完成后再定。
- 第一步实现：第一页所有文字临时隐藏，包括顶部 header（V—W 字标 + About/Work/Contact 导航）、kicker、大标题和 Cantor 图的 0/1 标注；用 `style.css` 末尾一个注释标明的临时 visibility 块实现，HTML 结构原样保留，方便之后决定文字回归方式。Cantor 曲线图形本身暂留，等新背景艺术确定后一并处理。
- 验证：Playwright MCP 实拍 1440×900 与 390×844，第一页仅剩纹理背景与 Cantor 曲线、无任何文字；console 0 error / 0 warning。

### 2026-07-19 — 第四页补回水深并重建真实反射
- 所有者实际检查后指出第一版天空主导方案仍像“天空贴图覆盖生态底图”：缺少水的表面感和纵深，水草基本不可见，云影也过于清晰、明亮和均匀。
- 原因：反射贡献曾全屏固定在约 72%–90%，既不随观察角度/画面远近变化，也没有水下深度散射；原生态底图还烘焙了一小块旧云影，提高透水率时会与新反射重叠。持续风浪只有很小 UV 位移，静止画面缺少可识别的表面线索。
- 实现：取消固定反射比例，改为近景约 27%、远景最高约 76% 的连续梯度，并叠加由水面法线和视线方向计算的 Fresnel 项。水底按亮度推断相对深度：浅水植物保留颜色和细节，深水区域增加水柱吸收、散射模糊；通过亮度与色度联合识别底图旧白云，只压暗云影而保留绿色植物。动态焦散只照亮水下层，进一步区分水底与表面。
- 反射改进：云体降低对比度并接受水色染色、粗糙度模糊和多尺度风浪位移；细窄表面亮纹从波面曲率计算。新增三处低频叶片滴水波，解析计算扩散波前并参与法线和光照；指针物理波场保留，但波幅调整得更克制。
- 验证：`npm run build` 通过；Playwright MCP 实拍 1440×900 与 390×844，水草在近景/右侧重新可见，远处保持更强天空反射，移动端无横向溢出。鼠标涟漪截图可见局部云影与水草同步弯折；`prefers-reduced-motion` 两次 canvas 截图 RMSE 为 0，console 0 error。

### 2026-07-19 — 第四页改为动态天空主导的真实水面
- 所有者反馈第四页植物仍过于显眼，希望大部分植物退到水面以下，并显著扩大蓝天白云倒影的占比；云需要动态漂移，鼠标涟漪应同时扭曲倒影并惊动水下游鱼，整体遵循物理直觉。
- 决策：保留 2026-07-17 确立的“原创写实资产 + 实时水膜”路线，但把单张生态底图拆成明确的反射/透射层。使用内置图像生成工具制作尺寸与生态底图一一对应的 1586×992 横版、941×1672 竖版原创蓝天积云纹理；生态图不再承担天空主反射。
- 实现：shader 从天空纹理的亮度/色度中提取云体，叠加两组不同速度的缓慢漂移，并以程序化连续蓝天填充背景；水面法线将反射 UV 与水下折射 UV 向相反方向扰动，反射贡献提高到约 72%–90%，生态层经水柱吸收、高光压缩和较弱折射后退入水下。物理波场增强了交互波幅、拖尾密度和衰减时间，涟漪会同步改变云影、透射景物、高光与明暗边。
- 生命交互：桌面和移动端均保留低存在感的纹理鱼；鱼会读取所在位置的波高和指针距离，受扰时向外转向、提高摆尾频率并产生克制的逃离位移，扰动消退后回到原游动路径。
- 验证：`npm run build` 通过；用 Playwright MCP 在 1440×900 与 390×844 视口实际渲染，确认天空倒影占据主体、植物仅隐约透出、桌面拖尾/点击涟漪可见且会扭曲云影、鱼会避开近距离波纹，移动端无横向溢出。console 0 error；截图产生的 WebGL `ReadPixels` warning 属于浏览器截图提示。

### 2026-07-17 — 第四页从全程序化场景切换为写实混合渲染
- 所有者明确表示当前第四页完全未达到预期。实际渲染核对确认问题属于技术路线而非参数：程序化 SDF 叶片、规则浮叶和水平循环鱼产生明显的图案/屏保感，所有层级又被统一绿色雾化；同时大量逐像素循环造成很高的 GPU 成本。
- 决策：停止继续扩充旧 fragment shader，改用“原创写实生态底图 + 实时水膜”的混合方案。使用内置图像生成工具制作并筛选了专用 16:10 桌面生态底图、9:16 移动生态底图和俯视透明鱼素材；`水塘参考/` 仅作审美参考，没有直接进入成品。
- 实现：重写 `src/pondScene.js`，移除程序化植物/云/鱼循环；新 shader 只负责正确比例的横竖底图采样、水面微波、局部折射/高光、纹理鱼、指针涟漪和鱼的避让。波场改为随视口比例变化的矩形网格，使传播和折射在横竖屏保持圆形；鱼只在桌面左下深水安全区出现，移动设备隐藏。新增静态底图 fallback、资源加载失败/WebGL context lost 降级、完整清理、离屏暂停，以及桌面/移动不同的波场、帧率和 DPR 策略；Three.js 水塘模块改为接近第四页时动态加载，不再阻塞首屏。
- 构图：桌面版把 Contact 和链接置于左侧深水负空间，水草与局部云影集中右侧和下缘；移动版使用独立竖构图，保留左侧暗水通道并限制浮叶只在右缘出现。
- 验证：`npm run build` 通过；Playwright MCP 在 1440×900 和 390×844 视口实际渲染，canvas 非空，桌面/移动构图正常、无横向溢出，圆形鼠标涟漪和桌面鱼运动有效，console 0 error。`prefers-reduced-motion` 下间隔 600 ms 的两次 canvas 像素对比差异为 0，确认静止模式有效。截图产生的 WebGL `ReadPixels` warning 属于浏览器像素读取提示，不是页面运行错误。

### 2026-06-23 — 根据水塘参考图重做第四页生态方向
- 所有者指出当前水塘“很丑、不自然”，并提供 `水塘参考/` 文件夹作为审美校准。参考图的核心不是天空倒影，而是明亮、清澈、俯视角度下的浅水生态系统：密集鲜绿植物群落、浮萍、大浮叶、细叶/羽毛状水草、小鱼和局部水面高光。
- 决策：阶段性暂停天空倒影优先级，先把水塘本体做好。第四页视觉重心从“水面反射场景”调整为“清澈俯视浅水生态”；天空倒影后续作为水面局部反射重新加入，不能压过水下生态。
- 实现：重写 `src/pondScene.js` 的水下生态层，加入程序化底草地毯、茎叶植物、羽毛状水草、浮萍/大浮叶、小白花/气泡感和小鱼点缀；反射权重降低，只保留透明水膜、轻微高光、焦散和涟漪。
- 验证：`npm run build` 通过；用 Playwright MCP 通过 Vite 服务实际渲染第四页，console 无 error / warning。

### 2026-06-23 — 第四页水草和小鱼细节优化
- 水草从粗暗条改为更细的多层叶片：每片叶子有独立高度、宽度、相位、摆动、叶脉和轻微高光，并限制过高叶片，避免过直、过假的绿色线条。
- 小鱼从简单鱼影改为更写实的程序化鱼：包含椭圆身体、尾鳍、眼点、侧线/鳃线和摆尾游动；鱼仍处于水下层，会被水色、折射和水草调和，不作为独立贴图漂在表面。
- 交互：鼠标经过水面时，除了产生涟漪，也会向 shader 传入指针位置；鱼会根据指针距离产生避让偏移，影响会在鼠标离开/停止后逐渐衰减。
- 修复：WebGL2 / GLSL3 中 `active` 是保留字，曾导致 shader 在 Three.js 注入前缀后编译失败；已将相关参数改名并用 Playwright 验证 console 无 shader warning。

### 2026-06-23 — 第四页迁移到 Vite + Three.js 并完成第一版水塘重构
- 工程已建立 Vite 技术栈：新增 `package.json`、`package-lock.json`、`vite.config.js`、`src/main.js`、`src/pondScene.js`；`index.html` 改为加载 Vite module 入口；旧 `scene.js` 暂留为历史参考但不再被页面引用。
- 第四页已从旧的单 WebGL shader 背景升级为 Three.js 管理的实时 GLSL 水塘场景：程序化蓝天/积雨云倒影、动态水面法线、反射/折射、水下水草、小鱼鱼影、焦散感，以及鼠标划过产生的涟漪。
- 验证：`npm run build` 通过；用 Playwright MCP 在桌面和移动视口实际渲染检查，第四页 canvas 正常显示，移动端文字无明显重叠，console 无 JavaScript error。浏览器曾出现截图触发的 WebGL `ReadPixels` 性能 warning，属于截图/像素读取引发的浏览器提示，不是页面运行错误。

### 2026-06-23 — 明确第四页开发范围与技术路线
- 将真实水塘相关需求明确归入“第四页开发”：真实水面、蓝天白云倒影、积雨云、水下水草、小鱼、鼠标涟漪、实时图形性能与 Playwright 渲染验收都属于第四页开发范围。
- 决策：第四页采用 Vite + Three.js + GLSL，WebGL2 作为主渲染基线；按工程升级、场景骨架、真实水面、天空云层、水下层、交互统一、性能验收的阶段推进。

### 2026-06-23 — 第四屏目标升级为真实雨后水塘场景
- 所有者明确第四屏的最终目标是“更加真实”：夏天下完雷阵雨后的小水塘，水面倒影蓝天白云，天空蓝而透彻，云是具有体积感的积雨云；同时可透过水面隐约看到随水摆动的水草和偶尔游过的小鱼；鼠标划过水面会产生涟漪，一切都应有物理性质和真实质感。
- 技术判断：当前原生 WebGL 单 shader 背景能做轻量反射和涟漪原型，但不适合长期承载完整的真实水塘场景。下一阶段应选择更强的实时图形技术栈，在静态站前提下引入 Three.js 管理场景和渲染管线，并继续使用 GLSL 自定义 shader 实现核心视觉。

### 2026-06-23 — 修复第四屏本地与 GitHub Pages 视觉不一致
- 原因：第四屏 WebGL 先生成程序化动态云层，但 `sky-cumulus-natural.webp` 加载成功后会切换/混合为照片纹理；GitHub Pages 上该图片正常加载，因此显示成照片，本地 `file://` 直接打开时则更容易看到未被照片覆盖的程序化动态云层。
- 决策：第四屏以动态程序化云层为准，移除照片纹理加载与混合逻辑，并给 `scene.js` 引用加版本号，让 GitHub Pages、localhost、本地文件打开的视觉保持一致。

### 2026-06-23 — 首页重构为「极简开发者主页」＋ favicon
- 首页重构为单页四屏滚动的极简开发者主页（主题色 纸白→红→绿→蓝），衬线字体 + 蓝色点缀，WebGL（`scene.js`）+ 纹理背景；含 Hero / About / Selected work / Contact 四区块（见 commit `90e3038` v1、`e9251c2` v2）。
- 新增内联 SVG favicon，消除 `favicon.ico` 404；用 Playwright MCP 逐屏渲染验证，四屏正常、console 0 报错。

### 2026-06-23 — 立项与基线
- 确定第一个开发项目为**个人主页**，定位为**静态网站**。
- 确定核心目标：**强表现力**的个人主页。
- 确定工作模式：**网页开发全权交给 agent**，所有者只把控目标与观感。
- 确定环境：WSL2 + VS Code + git/GitHub；**Codex 与 Claude Code 并用**。
- 待解决：具体技术栈、下一步设计方向。
