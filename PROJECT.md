# 项目文档：个人主页（Personal Homepage）

> 本文件是这个仓库的「单一事实来源（source of truth）」。
> **任何人或 AI agent 进入本仓库，请先完整读这份文件**，再开始工作——它说明了项目目标、关键决策和当前进度。
> 本文件是**追踪性**的：随着目标演变、开发推进，请持续更新第 5 节「当前状态」和第 7 节「决策与目标演变日志」。

---

## 1. 这是什么
- **项目**：我的个人主页（个人网站）。
- **类型**：静态网站（纯前端，无后端、无数据库）。
- **背景**：这是仓库所有者的**第一个开发项目**。仓库由一个 GitHub 仓库拉取而来，已有初步开发。

## 2. 目标与愿景
- 做一个**具有强烈表现力 / 视觉冲击力 / 交互有趣**的个人主页。
- 表现力优先：动画、富交互、（可能的）3D 等前端效果是重点。
- 整体气质 / 用途方向：整体的颜色以蓝色为主；网站应用展现个人的个性与艺术追求，并具有有趣的交互。
- 第四屏水面场景的长期目标：追求**真实、自然、具有物理感**的夏日雨后小水塘。画面应呈现雷阵雨后蓝而透彻的天空、具有体积感的积雨云、水面对蓝天白云的自然倒影、可透过水面隐约看到的水草、小鱼，以及鼠标划过水面时产生的涟漪互动。总体目标不是简单装饰性背景，而是一个有真实水面质感、深度层次和生命感的实时视觉场景。

## 3. 工作约定（给 agent 和协作者——本节最重要）
- **所有者不做网页开发，也不打算为这个项目学习网页开发。** 网页开发不在所有者的学习范围内。
- 因此：**实现细节全权交给 agent。** 所有者负责设定目标、判断最终的观感与效果；agent 负责所有技术选型与实现层面的决策。
- 在「静态网站」这个前提下，尽量把效果做得惊艳。
- 可以大胆改动——本项目由 git 管理，任何一步都能回滚。
- **每次有实质进展或做出决策后，请更新本文件**（第 5 节「当前状态」＋第 7 节日志），让下一个进来的人或 agent 不必重新摸索。

## 4. 技术决策

**已确定**
- 静态网站，无后端。
- 托管平台：GitHub Pages
- 设计方向 / 整体气质：颜色以蓝色为主；简约、高级、交互有趣；展现个性，具有几何、数学、自然美感。
- 第四页水塘场景应升级为独立的高表现力实时图形模块。现有原生 WebGL shader 可以保留为原型参考，但长期目标需要更强的场景组织、材质、后处理、交互和资源管理能力。
- 第四页开发技术栈确定为 **Vite + Three.js + 原生 GLSL shader**：
  - **Vite**：负责开发服务器、模块化、依赖管理、资源打包与缓存控制；构建产物仍是静态文件，可部署到 GitHub Pages。
  - **Three.js**：负责第四页实时场景的场景图、相机、渲染器、纹理/模型资源、后处理与交互管线。
  - **GLSL shader**：负责实时水面法线、折射、局部反射、高光、涟漪高度场和纹理鱼合成；植物、底床和静态天空倒影由原创生态底图提供。
  - **WebGL2 优先**：作为表现力与兼容性的基线。WebGPU 暂不作为主路线，以避免兼容性和工程复杂度过早上升。
- 第四页生态表现改为**原创写实底图 + 实时水膜的混合渲染**：植物、底床、遮挡和局部天空倒影由专门生成并筛选的横版/竖版生态底图提供；Three.js / GLSL 负责水面折射、微波、高光、涟漪和纹理鱼。停止在单个 fragment shader 中逐像素程序化生成整套植物、鱼和云层。

**待定（开放问题）**
- 是否在当前混合方案上继续增加独立表面浮叶层、反射/深度控制图和更复杂的鱼群行为；这些增强只有在当前整体真实感方向得到确认后再决定。

## 4.1 第四页开发

> 本节专门记录第四页（Contact / 水塘场景）的需求、问题边界与实现方案。凡是关于真实水面、蓝天白云倒影、水下水草、小鱼、鼠标涟漪和实时图形性能的讨论，都归入“第四页开发”。

**第四页的目标**
- 将当前第四页从“带动态倒影的背景”升级为“真实夏日雨后小水塘实时场景”。
- 画面预期：雷阵雨后，水塘倒影蓝天白云；天空蓝而透彻；云是具有体积感的积雨云；水面有真实波纹、微小扰动、高光、反射与折射；透过水面可隐约看到水下随水摆动的水草；偶尔有小鱼游过；鼠标划过水面时产生自然涟漪。
- 交互预期：鼠标/触控经过水面时影响水波，涟漪继续传播、衰减并影响倒影和水下折射。
- 真实感优先级高于短期实现速度；允许分阶段重建，不要求一次完成全部目标。

**当前第四页存在的问题**
- 旧版单个 fragment shader 同时程序化生成云、植物和鱼，既产生明显重复图案，也造成过高 GPU 成本；该路线已于 2026-07-17 被写实混合方案取代。
- 当前版本已具备原创横竖生态底图、局部天空倒影、轻量动态水膜、矩形物理波场、纹理鱼和鼠标/触控涟漪。
- 后续问题主要是审美精调：生态底图是否足够符合所有者预期、涟漪/水膜强度、鱼的存在感，以及是否需要把表面浮叶进一步拆成独立动态层。

**第四页分阶段方案（按当前混合路线）**
1. 工程升级：迁移到 Vite，安装并引入 Three.js，保持现有四屏页面结构和 GitHub Pages 静态部署能力。
2. 写实生态底层：为桌面和移动分别制作原创生态底图，让水草、底床、遮挡、深度和局部天空倒影先在静态画面中成立。
3. 实时水膜：用轻量 shader 处理动态法线、折射、局部反射、高光和矩形涟漪波场，不再在每个像素内生成整套生态。
4. 生命层：使用透明纹理鱼在深水安全区缓慢游动；是否增加独立浮叶层待整体方向确认后决定。
5. 物理感与交互统一：让鼠标/触控涟漪同时影响局部折射与明暗边，并让鱼对指针产生克制避让。
6. 性能与降级：按设备限制 DPR、波场短边和帧率；接近第四页时再动态加载 Three.js，不可见时停止渲染，失败时显示静态生态底图。
7. 验收：用 Playwright MCP 在桌面和移动视口实际渲染，确认 canvas 非空、构图正确、圆形涟漪有效、文字不重叠、console 无报错。

**第四页验收标准**
- 第一眼能明确识别为雨后小水塘，而不是抽象蓝色纹理。
- 水面同时具有倒影、透明感、波纹、高光与轻微扰动。
- 天空和云看起来自然，云有体积感和层次，倒影中的云不会像贴图硬盖在水面上。
- 水下水草和小鱼可被察觉，但不过度抢戏；它们应被水体颜色、折射和波纹自然调和。
- 鼠标划过水面时涟漪自然传播并逐渐衰减，能真实影响画面。
- 页面仍是静态站，部署到 GitHub Pages 后不依赖后端。

## 5. 当前状态
> 仓库已有初步开发
- 文件结构：单页 `index.html` + `style.css`；前端入口为 `src/main.js`，第四页水塘场景为 `src/pondScene.js`；第四页原创生态资源位于 `src/assets/pond/`，包含横版/竖版底图与透明鱼素材；构建配置为 `vite.config.js`，依赖记录在 `package.json` / `package-lock.json`。旧 `scene.js` 暂留为历史参考，页面不再加载。配套资源 `texture-carpet-titanium.webp`、`texture-sketch-paper.webp`、`texture-forest-floor.webp`。仓库根目录另有若干已提交但未引用的早期实验资源。
- 已完成：首页已重构为**「极简开发者主页」**——单页四屏滚动，主题色依次为 纸白 → 红 → 绿 → 蓝；衬线字体 + 蓝色点缀；前三页以 CSS/纹理和入口脚本动画为主，第四页由 Three.js/GLSL 实时水塘场景负责。四个区块：Hero（Vicious-W）/ About / Selected work（VKC_Study、c-learning）/ Contact（邮箱 + GitHub）。各区块为滚动触发渐显。
- 历史阶段曾修复：旧 `scene.js` 水面反射统一使用 WebGL shader 生成动态蓝天白云，并处理缓存不一致；该脚本现已退出运行链路，只保留作历史参考。
- 已补内联 SVG favicon（data URI，纸白底衬线「W」），消除 `favicon.ico` 404；用 Playwright 逐屏渲染核对，四屏正常、console 0 报错。
- 历史阶段已完成：项目迁移到 Vite + Three.js + GLSL，第一版 `pondScene` 曾使用程序化蓝天、植物和鱼；其中工程骨架与涟漪机制保留，程序化生态表现已被新版替换。
- 已完成：第四页水塘方向纠偏。根据 `水塘参考/` 中的参考图，第四页先暂时弱化天空倒影，优先重做“清澈俯视浅水生态”本体：密集底草、茎叶植物、羽毛状水草、浮萍/大浮叶、小白花/气泡感、小鱼点缀和透明水膜。
- 已完成：根据所有者对旧第四页“完全未达到预期”的反馈，第四页完成第二次路线级重建。旧版程序化叶片、浮叶、鱼和 FBM 云层已删除，改用原创写实横版/竖版生态底图、透明纹理鱼与轻量实时水膜；桌面文字区移到左侧深水负空间，移动端使用独立竖构图。
- 已完成：保留并优化涟漪物理交互；桌面/移动的矩形波场短边分别为 160 / 112 像素并以 30 Hz 模拟，波场随视口比例保持圆形传播；同时限制 DPR、离屏停止渲染，并加入静态底图、WebGL 创建失败和 context lost 降级。
- 进行中：新版混合水塘等待所有者对整体观感进行判断；后续根据观感反馈继续调整生态底图、鱼的存在感、局部天空倒影和水膜强度，不再回到全程序化植物路线。
- 下一步：先确认新版是否已达到正确的真实感方向，再决定是否增加独立表面浮叶层、反射控制图或更细致的鱼群行为。

## 6. 环境与工具链
- 系统：Windows + WSL2（Ubuntu）。
- 编辑器：VS Code（在 WSL 终端用 `code .` 启动）。
- 版本控制：git + GitHub（所有者的每个项目都会建立仓库并用 git 管理）。
- 在用的 agent：Codex、Claude Code。
- 安装依赖：`npm install`
- 本地开发预览：`npm run dev -- --port 8000`（若 8000 被占用，Vite 会自动尝试下一个端口）
- 生产构建：`npm run build`

## 7. 决策与目标演变日志（追加式，最新在最上面）
> 每当目标发生变化、做出一项技术决策、或完成一个阶段性开发，就在此追加一条，并标注日期。
> 这一节是这个项目的「记忆」——它记录我们是怎么一步步走到现在的。

### 2026-07-17 — 第四页从全程序化场景切换为写实混合渲染
- 所有者明确表示当前第四页完全未达到预期。实际渲染核对确认问题属于技术路线而非参数：程序化 SDF 叶片、规则浮叶和水平循环鱼产生明显的图案/屏保感，所有层级又被统一绿色雾化；同时大量逐像素循环造成很高的 GPU 成本。
- 决策：停止继续扩充旧 fragment shader，改用“原创写实生态底图 + 实时水膜”的混合方案。使用内置图像生成工具制作并筛选了专用 16:10 桌面生态底图、9:16 移动生态底图和俯视透明鱼素材；`水塘参考/` 仅作审美参考，没有直接进入成品。
- 实现：重写 `src/pondScene.js`，移除程序化植物/云/鱼循环；新 shader 只负责正确比例的横竖底图采样、水面微波、局部折射/高光、纹理鱼、指针涟漪和鱼的避让。波场改为随视口比例变化的矩形网格，使传播和折射在横竖屏保持圆形；鱼只在桌面左下深水安全区出现，移动设备隐藏。新增静态底图 fallback、资源加载失败/WebGL context lost 降级、完整清理、离屏暂停，以及桌面/移动不同的波场、帧率和 DPR 策略；Three.js 水塘模块改为接近第四页时动态加载，不再阻塞首屏。
- 构图：桌面版把 Contact 和链接置于左侧深水负空间，水草与局部云影集中右侧和下缘；移动版使用独立竖构图，保留左侧暗水通道并限制浮叶只在右缘出现。
- 验证：`npm run build` 通过；Playwright MCP 在 1440×900 和 390×844 视口实际渲染，canvas 非空，桌面/移动构图正常、无横向溢出，圆形鼠标涟漪和桌面鱼运动有效，console 0 error。`prefers-reduced-motion` 下间隔 600 ms 的两次 canvas 像素对比差异为 0，确认静止模式有效。截图产生的 WebGL `ReadPixels` warning 属于浏览器像素读取提示，不是页面运行错误。

### 2026-06-23 — 根据水塘参考图重做第四页生态方向
- 所有者指出当前水塘“很丑、不自然”，并提供 `水塘参考/` 文件夹作为审美校准。参考图的核心不是天空倒影，而是明亮、清澈、俯视角度下的浅水生态系统：密集鲜绿植物群落、浮萍、大浮叶、细叶/羽毛状水草、小鱼和局部水面高光。
- 决策：阶段性暂停天空倒影优先级，先把水塘本体做好。第四页视觉重心从“水面反射场景”调整为“清澈俯视浅水生态”；天空倒影后续作为水面局部反射重新加入，不能压过水下生态。
- 实现：重写 `src/pondScene.js` 的水下生态层，加入程序化底草地毯、茎叶植物、羽毛状水草、浮萍/大浮叶、小白花/气泡感和小鱼点缀；反射权重降低，只保留透明水膜、轻微高光、焦散和涟漪。
- 验证：`npm run build` 通过；用 Playwright MCP 通过 Vite 服务实际渲染第四页，console 无 error / warning。

### 2026-06-23 — 第四页水草和小鱼细节优化
- 水草从粗暗条改为更细的多层叶片：每片叶子有独立高度、宽度、相位、摆动、叶脉和轻微高光，并限制过高叶片，避免过直、过假的绿色线条。
- 小鱼从简单鱼影改为更写实的程序化鱼：包含椭圆身体、尾鳍、眼点、侧线/鳃线和摆尾游动；鱼仍处于水下层，会被水色、折射和水草调和，不作为独立贴图漂在表面。
- 交互：鼠标经过水面时，除了产生涟漪，也会向 shader 传入指针位置；鱼会根据指针距离产生避让偏移，影响会在鼠标离开/停止后逐渐衰减。
- 修复：WebGL2 / GLSL3 中 `active` 是保留字，曾导致 shader 在 Three.js 注入前缀后编译失败；已将相关参数改名并用 Playwright 验证 console 无 shader warning。

### 2026-06-23 — 第四页迁移到 Vite + Three.js 并完成第一版水塘重构
- 工程已建立 Vite 技术栈：新增 `package.json`、`package-lock.json`、`vite.config.js`、`src/main.js`、`src/pondScene.js`；`index.html` 改为加载 Vite module 入口；旧 `scene.js` 暂留为历史参考但不再被页面引用。
- 第四页已从旧的单 WebGL shader 背景升级为 Three.js 管理的实时 GLSL 水塘场景：程序化蓝天/积雨云倒影、动态水面法线、反射/折射、水下水草、小鱼鱼影、焦散感，以及鼠标划过产生的涟漪。
- 验证：`npm run build` 通过；用 Playwright MCP 在桌面和移动视口实际渲染检查，第四页 canvas 正常显示，移动端文字无明显重叠，console 无 JavaScript error。浏览器曾出现截图触发的 WebGL `ReadPixels` 性能 warning，属于截图/像素读取引发的浏览器提示，不是页面运行错误。

### 2026-06-23 — 明确第四页开发范围与技术路线
- 将真实水塘相关需求明确归入“第四页开发”：真实水面、蓝天白云倒影、积雨云、水下水草、小鱼、鼠标涟漪、实时图形性能与 Playwright 渲染验收都属于第四页开发范围。
- 决策：第四页采用 Vite + Three.js + GLSL，WebGL2 作为主渲染基线；按工程升级、场景骨架、真实水面、天空云层、水下层、交互统一、性能验收的阶段推进。

### 2026-06-23 — 第四屏目标升级为真实雨后水塘场景
- 所有者明确第四屏的最终目标是“更加真实”：夏天下完雷阵雨后的小水塘，水面倒影蓝天白云，天空蓝而透彻，云是具有体积感的积雨云；同时可透过水面隐约看到随水摆动的水草和偶尔游过的小鱼；鼠标划过水面会产生涟漪，一切都应有物理性质和真实质感。
- 技术判断：当前原生 WebGL 单 shader 背景能做轻量反射和涟漪原型，但不适合长期承载完整的真实水塘场景。下一阶段应选择更强的实时图形技术栈，在静态站前提下引入 Three.js 管理场景和渲染管线，并继续使用 GLSL 自定义 shader 实现核心视觉。

### 2026-06-23 — 修复第四屏本地与 GitHub Pages 视觉不一致
- 原因：第四屏 WebGL 先生成程序化动态云层，但 `sky-cumulus-natural.webp` 加载成功后会切换/混合为照片纹理；GitHub Pages 上该图片正常加载，因此显示成照片，本地 `file://` 直接打开时则更容易看到未被照片覆盖的程序化动态云层。
- 决策：第四屏以动态程序化云层为准，移除照片纹理加载与混合逻辑，并给 `scene.js` 引用加版本号，让 GitHub Pages、localhost、本地文件打开的视觉保持一致。

### 2026-06-23 — 首页重构为「极简开发者主页」＋ favicon
- 首页重构为单页四屏滚动的极简开发者主页（主题色 纸白→红→绿→蓝），衬线字体 + 蓝色点缀，WebGL（`scene.js`）+ 纹理背景；含 Hero / About / Selected work / Contact 四区块（见 commit `90e3038` v1、`e9251c2` v2）。
- 新增内联 SVG favicon，消除 `favicon.ico` 404；用 Playwright MCP 逐屏渲染验证，四屏正常、console 0 报错。

### 2026-06-23 — 立项与基线
- 确定第一个开发项目为**个人主页**，定位为**静态网站**。
- 确定核心目标：**强表现力**的个人主页。
- 确定工作模式：**网页开发全权交给 agent**，所有者只把控目标与观感。
- 确定环境：WSL2 + VS Code + git/GitHub；**Codex 与 Claude Code 并用**。
- 待解决：具体技术栈、下一步设计方向。
