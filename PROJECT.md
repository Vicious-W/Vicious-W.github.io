# 项目事实

## 项目定义

- 单页静态网站，入口是无文字的三维场景选择空间；
- 已命名场景有两个：已实现并由所有者验收的 `SOURCE`，以及当前准备开发的 `FLY`；
- 蓝色是全站核心颜色；自然物体仍保留符合真实环境的颜色、光照和材质；
- 网站不使用介绍性文案、传统导航或内容区；`FLY` 只允许飞行器操作指南和真实设备
  原生标签、刻度、警告及仪表数字出现必要文字；
- 主要内容必须是可交互的真实三维物理模型，视觉、声音和粒子表现必须由可解释的
  世界状态或物理事件驱动。

## 已实现：SOURCE

`SOURCE` 是蓝色 Pavia TRIGA Mark II 核反应堆实验室。所有者已于 2026-07-31
接受相机改进后的场景；接受检查点为 `def8383`，对应最终业务实现提交为 `97821a3`。
后续任务默认不得改动 SOURCE，只有所有者明确要求或发现跨场景回归时才进入其业务范围。

主要实现：

- `src/scenes/reactor/physicalScene.js`：SOURCE 场景、玻璃刚体、格栅物理、轻水/损伤
  耦合、控制台拾取、控制权分流和观察相机；
- `src/scenes/reactor/sessionController.js`：AUTO 与 MANUAL 共用的反应堆状态、命令、
  联锁、点堆动力学和两节点热工状态；
- `src/scenes/reactor/autoProgram.js`：使用真实设备指令和联锁推进的 AUTO 连续程序；
- `src/scenes/reactor/controlConsole.js`、`autoConsole.js`：物理独立、共享状态的双控制台；
- `src/scenes/reactor/reactorModel.js`：Pavia TRIGA Mark II 池体、堆芯、控制棒、实验
  设施、仪器、冷却和连接结构；
- `src/scenes/reactor/labEnvironment.js`、`undergroundPlant.js`：实验大厅与地下设备；
- `src/scenes/reactor/waterSystem.js`、`cherenkov.js`：独立轻水、高度场、光学、热羽流
  与功率驱动的局部切伦科夫辉光；
- `src/scenes/reactor/glassArchitecture.js`、`glassDamage.js`、`glassAudio.js`：玻璃建筑、
  动态玻璃、耐久、破碎和接触声音；
- `src/scenes/reactor/reactorAudio.js`：控制棒、气动机构、泵和水体冲量的状态声音；
- `src/scenes/reactor/freeCamera.js`：中心焦点轨道、平滑推进、方向键平移和水下/地下
  可达的非碰撞观察相机。

SOURCE 的完整事实、近似和真实性边界保存在 `docs/engineering/SOURCE_SCENE.md`、
`REACTOR_POOL_SYSTEM.md`、`REACTOR_MODEL.md` 与 `SOURCE_LAB_OPTICS.md`。

## 当前目标：FLY

`FLY` 是以真实大气、气象和飞行器为核心的无限天空旅程。当前代码已建立第一阶段
“晴空 + Cameron C-100 参考热气球”纵向切片；第四轮实现已处理互斥兼容配对切换、
多注册项键盘遍历和重叠预览指针消歧问题，正在等待审查。后续类别仍是
锁定目标，不能把第一阶段实现误写成全部 FLY 已完成。

当前第一阶段实现：

- `src/core/sceneHost.js`：单 canvas、单活动场景的 SITE_SELECT / SOURCE / FLY 宿主与
  资源代次；
- `src/scenes/selector/selectorScene.js`：无文字实时三维 SOURCE / FLY 缩影入口；
- `src/scenes/fly/`：天气/飞行器注册、`1/120 s` 权威仿真、标准大气、确定性分层风、
  程序化区块与浮动原点、C-100 双体热气球、指南/触控/相机、状态音频和自动回收；
- SOURCE 只增加 `sourceScene.js` 生命周期包装与遗漏调试钩子的 dispose 清理，既有业务
  模型未改写。

### 体验定义

1. 用户进入网站后先在无文字三维空间中选择 `SOURCE` 或 `FLY`；
2. 进入 FLY 后选择气象和飞行器；
3. 选择飞行器时显示该型号专属操作指南，这是普通网页文字限制的唯一说明性例外；
4. 用户操作物理启动装置出发，以驾驶员身份手动驾驶；
5. 飞行器提供符合其真实能力的自动结束方案：普通飞机返航并着陆，热气球寻找安全
   地点降落和回收，航天飞机进近滑翔着陆；
6. 旅程在程序化生成的天空、地面/海洋、云层和天气中连续进行。

### 统一物理引擎

FLY 使用一个项目级权威仿真，而不是若干互不相干的动画或物理库：

- 一个固定时间步和一个权威世界状态；
- 大气、风场、气象、刚体、空气动力、推进、热力、结构、碰撞、声音与粒子由独立
  模块计算，但通过同一状态耦合；
- 飞行器受到的是相对空气速度、密度、压力、温度、湍流和降水产生的力与力矩；
- 视觉、声音、振动和损伤读取物理结果，不能反过来用装饰动画冒充动力学；
- 无限空间通过确定性区块、局部高精度物理区域、分级细节和浮动原点实现；不会创建
  无限数量的刚体或假装浏览器能够进行全球全尺度流体求解；
- 真实飞行器以具体资料原型建模；UFO 等虚构飞行器必须标记为虚构，并建立内部一致
  的推进、惯性和能源规则，不冒充现实资料。

### 第一阶段

第一阶段只建立可完整体验的“晴空 + 热气球”纵向切片：

- 多场景入口、生命周期、资源释放和 SOURCE 回归保护；
- FLY 的无文字气象/飞行器选择空间、热气球操作指南和出发流程；
- 标准大气、晴空、分层风场、热气流、地平线和可持续生成的地表环境；
- 以 Cameron C-100 为首个包络几何参考的热气球系统；
- 包络、承力带/悬索、燃烧器框架、燃烧器、燃料瓶、篮筐和控制绳分别建模；
- 内外空气密度、燃烧加热、热损失、浮力、阻力、风层漂移、燃料消耗、吊篮摆动、
  起飞和着陆构成同一连续物理过程；
- 用户手动控制燃烧器和放气，不获得虚构的水平推力；
- 自动功能根据风层、地形、障碍和燃料寻找可达安全地点并真实降落，不瞬移回起点；
- 相对风、燃烧器、球囊织物、篮筐、绳索与着陆声音由状态驱动。

后续才增加固定翼、喷气式、旋翼、航天飞机、UFO，以及多云、雷暴和台风。不同类别
共享世界与仿真时钟，但必须使用各自真实的飞行和推进模型，不能换皮复用同一飞行函数。

正式目标和验收见 `PROJECT_SPEC.md`，统一物理工程基线见
`docs/engineering/FLY_PHYSICS.md`，多场景和体验架构见
`docs/engineering/FLY_SCENE_ARCHITECTURE.md`，当前执行切片见 `.agent/next-task.md`。

## 技术结构

当前已使用：

- Vite；
- Three.js；
- cannon-es；
- Web Audio API；
- `npm test` 的 Node 逻辑测试；
- 纯前端静态构建，可部署到 GitHub Pages；
- 无后端、数据库、账号或运行时密钥。

FLY 可以在经过任务内论证后扩展底层模块，但不得为了“复杂”盲目并列多个权威刚体
世界。新增依赖必须解决明确问题，并保持静态部署和离线确定性测试能力。

## 建模与真实性规则

- 主要物体必须有真实三维几何结构，不能用照片、背景图或预渲染画面代替；
- shader 可以表达材质、大气、体积云、水体、辉光和光学，不能代替主要结构；
- 纹理只能提供表面细节，不能承担物体主要形状；
- 运动、声音、振动、损伤和粒子必须由可解释状态或物理事件驱动；
- 实际资料、所有者艺术方向和工程代理必须明确区分，不能把近似冒充精确事实；
- 浏览器无法实时求解的全球流体、微尺度湍流和材料连续体可以采用经过说明、量纲正确、
  可验证的工程模型；近似不能退化为与状态无关的循环动画；
- SOURCE 与 FLY 分别拥有独立会话和资源；切换场景必须销毁上一场景的计时器、音频、
  监听器、物理对象和 GPU 资源，不能让两个场景在后台继续运行。

## Agent 协作结构

- Agent 身份为 `GENERAL`、`IMPLEMENTER` 与 `REVIEWER`；GENERAL 同时承担普通协作、
  控制面维护和轮转监督；
- 角色不与 Claude Code 或 Codex 绑定，父脚本为每次调用记录执行器、模型、effort、
  权限和任务边界；
- 实现和审查严格串行；同一任务内各角色恢复自己的逻辑会话；
- 一次轮回固定为一份完成的 IMPLEMENTER 交付；正常只有一个实现段，主实现者遇到
  真实额度上限时可单向交给预配置的 Codex 后继段，仍不增加轮回编号；
- 后继使用新的 IMPLEMENTER 会话和结构化 Git 交接，前任会话被归档并标记为
  `SUPERSEDED`，不并行、不回切；审查只出现在相邻两次实现之间，最后一轮由所有者查看；
- `agent-cycle.sh` 管理有界轮回，`agent-supervisor.sh` 管理跨额度窗口恢复和异常交接；
- 自动调用在 `.agent/artifacts/runs/` 留下运行清单与用量证据。

## 文档规则

`PROJECT.md` 只记录当前事实、现行决定和立即目标，不追加开发日记。Git 保存历史；
详细物理过程保存在工程文档，不能为了简短而删掉实现真实性所需的结构、公式、来源、
近似和验收条件。
