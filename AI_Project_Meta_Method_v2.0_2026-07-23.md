# 面向长期项目的人—多 Agent 协作元方法

版本：v2.0

日期：2026-07-23

状态：第二版正式方法论

## 摘要

长期项目应独立存在，Agent 只是进入项目履行职责的临时执行者。多 Agent 的价值
不来自“同时多开几个聪明助手”，也不来自轮流使用不同品牌，而来自把实现、验证、
审查和产品决策分成相互制衡的制度角色，并用中立、可验证、有上限的控制流程连接
这些角色。

v2.0 的核心结论是：

> 多 Agent 的优势来自角色、权限、证据和责任的分离，而不是并发本身。

对于会持续改变真实文件的项目，推荐的最小可靠结构是：

- 人：目标所有者和最终产品判断者；
- 实现 Agent：唯一被授权修改业务状态的执行者；
- 审查 Agent：不参与实现、依据正式标准独立验收；
- 中立控制面：串行启动两者、检查交付物、限制轮数并处理故障；
- 本地项目与 Git：唯一可持续、可追溯的事实载体。

## 一、v1.0 中应继续保留的地基

v1.0 确立的以下原则仍然成立：

1. **项目高于 Agent。** 项目不依赖任何单一 Agent 的会话或隐式记忆。
2. **长期项目应本地化。** 涉及真实文件、代码、数据和持续修改的工作，以本地
   项目为核心。
3. **记忆应显式化。** 目标、状态、决策和历史保存在文件、结构和版本记录中。
4. **区分理解与改变状态。** 知识讨论和项目操作是不同工作。
5. **只沉淀有效成果。** 不机械保存全部聊天，只保存改变理解、决策或项目状态的
   内容。

v2.0 不是推翻这些原则，而是回答 v1.0 尚未解决的问题：

- 多个操作型 Agent 进入同一项目时，为什么经常没有比单 Agent 更好？
- Agent 应怎样拥有不同身份，而不只是不同名字？
- 怎样自动交接，同时保证最多只有一个 Agent 修改项目？
- 父流程怎样知道一个 Agent 真正结束，而不是只听它说“完成了”？
- 权限问题怎样避免无人值守时卡住？
- 怎样限制自动循环，不让两个 Agent 无限互相退回？
- 人怎样从逐步监督者转变为目标和产品判断的所有者？

## 二、从实践中发现的 v1.0 局限

### 1. “多个通用帮手”不等于多 Agent 系统

当不同 Agent：

- 读取同一份项目记忆；
- 拥有相同修改权限；
- 面对相同目标；
- 产出相同类型的代码或文档；
- 都以“把项目继续做下去”为身份；

它们事实上是可互换的通用执行者。此时更换 Agent 只是在更换实现习惯，并没有
建立新的项目能力。

### 2. 共享身份会放大不一致

不同模型在代码组织、命名、审美、风险偏好和验证习惯上并不完全一致。如果它们
都被授权以同一身份修改项目，项目会承受额外的上下文重建、风格漂移、重复探索和
相互推翻，却得不到稳定的专业分工收益。

### 3. 并发不是默认优势

真实项目中的写操作共享文件、Git 状态、开发服务器、依赖和测试环境。两个写入者
同时工作容易造成冲突、污染基线和难以归因的故障。即使工具技术上支持并发，也
不代表项目适合并发写入。

### 4. Agent 自己互相启动会破坏角色边界

让实现者在结束时启动审查者，再让审查者启动下一个实现者，看似自动，实际会产生：

- 两个 Agent 生命周期短暂重叠；
- 谁拥有父进程、终止权和错误处理权不明确；
- Agent 既做业务判断又控制流程；
- 额度、权限或异常退出时难以恢复；
- 循环次数难以可靠控制。

### 5. 自然语言“完成了”不是可验证的状态

Agent 可能正常退出但没有改动，可能改了文件却没有报告，也可能声称通过而测试失败。
流程不能只依据对话文本进入下一阶段。

## 三、v2.0 的基本原则

### 原则 1：项目是唯一持续存在的主体

Agent 会启动、退出、更换或失去上下文，项目必须独立保存：

- 当前正式目标；
- 范围和验收标准；
- 技术背景与决策；
- 当前任务和轮次；
- 实现交接；
- 审查结论；
- 验证证据；
- Git 历史；
- 自动流程的停止原因。

### 原则 2：身份由制度定义，不由模型品牌定义

一个 Agent 的项目身份应由下式定义：

```text
身份 = 职责 + 权限 + 必读输入 + 必交输出 + 验收标准 + 停止条件
```

“Claude”“Codex”只是当前承载角色的工具。真正不可随意互换的是“实现者”
“独立审查者”等制度身份。

### 原则 3：实现权与验收权分离

实现者负责做出技术方案并改变业务状态；审查者负责依据所有者批准的标准判断结果，
不直接替实现者修复。这样可以减少自我确认偏差，并让失败的责任和下一步清晰可见。

### 原则 4：单一写入者

任何时刻只允许一个角色修改业务项目。审查者默认只读；中立脚本只修改约定的状态、
报告归档和 Git 检查点。

若某些研究或分析可以并行，也必须满足：

- 不修改共享业务文件；
- 输出位置互不冲突；
- 输入基线固定；
- 有明确的汇总者；
- 并发带来的收益大于协调成本。

否则保持串行。

### 原则 5：控制面与业务面分离

控制面负责“谁何时运行、能做什么、怎样结束”；业务 Agent 负责“具体实现或审查
什么”。Agent 不应该负责启动自己的继任者。

### 原则 6：交接必须基于证据

进入下一阶段至少需要同时满足：

- 子进程已经退出；
- 退出码符合预期；
- 必需交付文件存在且格式有效；
- Git 和受保护文件没有越界变化；
- 已配置验证有真实结果；
- 工作区状态符合下一阶段要求。

### 原则 7：所有自动循环必须有上限

自动化是有限状态机，不是两个 Agent 的无限对话。最大轮数、重复问题、产品决策、
权限失败、额度失败、超时和无法验证都必须是明确停止条件。

### 原则 8：无人值守依赖“预授权或立即失败”

不要给 Agent 整机全权，也不要让权限弹窗无限等待。应提前配置完成任务所需的最小
权限；未授权能力在无人值守时立即失败、保存证据并把控制权交还给人。

### 原则 9：人负责目标，不负责逐命令监管

人主要负责：

- 说明想要什么；
- 决定什么算可接受；
- 作出范围和产品取舍；
- 处理自动系统不能决定的冲突；
- 进行最终主观体验。

人不应被迫持续盯着 Agent 的每次命令或手工转发每一轮报告。

## 四、最小可靠角色体系

### 1. 项目所有者

职责：

- 提出阶段目标和优先级；
- 批准范围、验收标准和风险边界；
- 决定是否启动自动循环；
- 处理审查者无法替代的主观判断；
- 在达到循环上限或需要产品决策时接管。

不应被自动化替代的事项：

- 产品方向；
- 审美和手感的最终接受；
- 重大范围扩张；
- 发布、部署或不可逆操作授权；
- 是否接受已知风险。

### 2. 实现 Agent

职责：

- 阅读正式目标、最近审查和项目背景；
- 研究、设计并实现当前范围；
- 运行要求的验证；
- 记录变更、证据、剩余风险和审查重点；
- 完成一轮后退出。

典型权限：

- 读写业务代码、样式、测试和允许的项目文档；
- 运行构建、测试和本地预览；
- 使用任务所需的公开检索和浏览器工具；
- 不控制 Git 历史，不修改角色规则，不启动其他 Agent。

### 3. 独立审查 Agent

职责：

- 对照正式目标和验收契约检查实现；
- 检查功能、回归、边界、测试和实际界面；
- 为问题提供可复现证据、影响和客观验收条件；
- 输出 PASS 或 CHANGES_REQUIRED；
- 完成一轮后退出。

典型权限：

- 仓库只读；
- 运行非破坏性验证；
- 使用浏览器检查；
- 正式报告由外层脚本代为写入和归档；
- 不修改业务代码，不自创需求，不启动实现者。

### 4. 中立控制面

它通常是确定性的本地脚本，而不是第三个会自由判断业务的 Agent。

职责：

- 检查前置条件；
- 获取独占锁；
- 按角色注入明确提示和权限；
- 串行启动子进程；
- 监督心跳、超时和终止；
- 根据退出码和交付物验证阶段完成；
- 运行统一验证；
- 创建本地 Git 检查点；
- 更新最小状态；
- 决定继续、通过或停止；
- 生成面向人的简报。

控制面不应：

- 自己决定业务实现；
- 修改验收标准；
- 忽略失败继续运行；
- 自动推送或部署；
- 自动丢弃异常时留下的文件；
- 无限制重试。

### 5. 可选专业角色

只有当角色拥有独特职责和交付物时才增加，例如：

- 资料研究者：只输出来源、事实和不确定性；
- 安全审查者：只检查威胁模型与安全边界；
- 性能审查者：只提供可重复测量与瓶颈证据；
- 视觉设计者：提供明确视觉规范，不直接与实现者争夺业务文件。

如果新角色与现有角色拥有相同输入、权限和输出，就不应增加。

## 五、项目的分层事实结构

推荐把信息分成以下层次：

| 层 | 回答的问题 | 典型载体 |
| --- | --- | --- |
| 所有者意图 | 我们为什么做、最终想得到什么 | 产品说明、阶段目标 |
| 正式规范 | 当前范围和客观验收是什么 | `PROJECT_SPEC.md` |
| 角色契约 | 每个角色能做什么、不能做什么 | `AGENTS.md`、角色说明 |
| 审查契约 | 问题怎样分级、怎样判定 PASS | `REVIEW_CONTRACT.md` |
| 活动任务 | 当前这一轮具体执行什么 | 任务文件 |
| 机器状态 | 任务 ID、轮次、最近提交和结论 | 状态文件 |
| 实现交接 | 实现者做了什么、如何验证 | 实现报告 |
| 审查交接 | 发现了什么、下一轮必须满足什么 | 最新审查和历史归档 |
| 运行证据 | 构建、测试、截图、日志、停止原因 | artifacts |
| 历史 | 每次有效状态变化 | Git 提交 |

信息优先级应明确。推荐顺序：

```text
所有者最新明确指令
  > 当前正式规范
  > 角色/审查契约
  > 当前任务
  > 项目技术记忆
  > Agent 的临时推断
```

技术历史不能覆盖当前已更新的正式目标。

## 六、自动循环应被设计成有限状态机

### 1. 推荐状态流

```text
OWNER_READY
    │
    ▼
PREFLIGHT ──失败──> STOP_FOR_OWNER
    │通过
    ▼
IMPLEMENT ──异常──> PRESERVE_AND_STOP
    │进程退出 + 交付有效
    ▼
VALIDATE_AND_CHECKPOINT ──越权/检查点失败──> PRESERVE_AND_STOP
    │
    ▼
REVIEW ──异常/报告无效──> KEEP_PREVIOUS_REVIEW_AND_STOP
    │有效结论
    ├── PASS ───────────> COMPLETE
    │
    └── CHANGES_REQUIRED
             │
             ├── 达到上限/重复问题/需产品决策 ──> STOP_FOR_OWNER
             │
             └── 未达到上限 ───────────────────> IMPLEMENT
```

### 2. 父脚本怎样知道 Agent 已结束

父脚本必须以操作系统子进程为事实，而不是解析“我完成了”这句话：

1. 启动 Agent CLI 并记录 PID/进程组；
2. 等待子进程退出；
3. 记录退出码、耗时和日志；
4. 超时时终止整个进程组；
5. 分类额度、认证、权限、浏览器等失败；
6. 检查必交报告、文件变化和 Git 边界；
7. 只有全部机械条件成立才进入下一状态。

进程退出只是必要条件，不是充分条件。

### 3. 为什么不让 Agent 相互启动

推荐由人启动一次父脚本，然后父脚本依次启动 Agent。这样：

- 父脚本始终拥有完整生命周期；
- Claude 完全退出后才会启动 Codex，没有不可避免的 Agent 重叠；
- 轮数和超时由一个地方控制；
- 某一阶段失败时不会误启动下一阶段；
- 中断信号可以传递到已知进程组；
- 自动化不依赖 Agent 是否记得调用下一条命令。

父脚本持续存在不违反“同一时间只运行一个 Agent”，因为它是控制程序，不是第二个
自由修改项目的智能执行者。

### 4. 独占锁

所有能启动实现或审查的入口都应争用同一把项目锁。锁至少保存：

- 父进程 PID；
- 进程启动时间身份；
- 当前命令；
- 启动时间。

第二个入口发现活动锁时立即拒绝。陈旧锁可以在确认原进程不存在且 PID 未被复用后
回收，不能仅凭一个旧 PID 文件判断。

## 七、每一轮的交接契约

### 1. 实现者交接

实现报告至少包含：

- 基准提交；
- 本轮目标；
- 实际变更；
- 关键技术决策及原因；
- 资料来源和合理抽象；
- 已运行的验证及真实结果；
- 未配置、未执行或无法验证的部分；
- 已知风险；
- 审查者应重点检查什么。

父脚本应额外验证：

- Git 历史和配置未被实现者修改；
- 没有预暂存内容；
- 受保护控制面未变化；
- 实现报告确实被更新；
- 存在有意义的仓库改动；
- 统一验证已运行；
- 本地实现检查点成功创建。

### 2. 审查者交接

审查报告至少包含：

- 被审查提交和对比基准；
- 工作区状态；
- 验证结果；
- 按严重等级组织的问题；
- 每个问题的要求依据、证据、影响、复现和验收条件；
- 已确认工作的部分；
- 未验证部分；
- 下一步必做事项；
- 唯一机器可读结论：PASS 或 CHANGES_REQUIRED。

父脚本应验证：

- 报告存在且非空；
- 格式和必需章节完整；
- 结论唯一且合法；
- 只出现允许的报告/状态变化；
- 历史报告不被覆盖；
- 当前状态与结论一致；
- 审查检查点成功创建。

### 3. Git 是状态机的检查点，不是 Agent 的自由工具

推荐由中立包装器在阶段边界创建本地提交：

```text
实现检查点：agent: implementation round N
审查检查点：agent: review round N
```

这样每一轮都能回答：

- 实现者到底改了什么；
- 审查者审查的是哪个精确版本；
- 哪些问题属于哪一轮；
- 中断后应从哪里恢复。

自动提交不等于自动发布。push 和部署应属于单独、明确授权的发布流程。

## 八、权限方法：够用、非交互、可审计

### 1. 不采用“整个项目和整台机器全权”

无人值守需要消除弹窗，但不需要授予无限权限。最小权限应覆盖：

- 项目内允许的读写范围；
- 必需的构建、测试和本地预览；
- 只读 Git 检查；
- 公开资料检索；
- 浏览器/MCP 验证；
- 专用、可写的缓存目录。

明确拒绝：

- push、部署和线上写入；
- reset、clean、rebase、强制 checkout；
- 修改 Git refs、hooks 或本地控制配置；
- sudo 和系统级安装；
- 输出全部环境变量或读取凭据目录；
- 自行启动其他 Agent；
- 修改角色契约和审查历史。

### 2. 无人值守模式

权限策略应是：

```text
已预批的能力：直接执行
未预批的能力：立即拒绝并退出
```

不应是：

```text
未预批的能力：弹窗并无限等待所有者
```

父脚本把权限失败记录为确定性停止原因，人回来后可以判断是补充最小权限、调整任务，
还是拒绝该行为。

### 3. 启动前预检

预检不启动任何真实 Agent，至少检查：

- 必需 CLI 和脚本；
- 角色/规范/任务/状态文件；
- 权限配置语法和危险授权；
- 任务状态与轮数；
- Git 工作区和本地提交能力；
- 依赖完整性；
- Agent 登录；
- MCP/浏览器健康；
- 模型和超时配置合法性。

预检失败时不进入“先启动再看”的状态。

## 九、模型与推理强度的选择

### 1. 按角色和任务选，不按“最强就是最好”选

实现者通常需要：

- 较强的长程编码、工具使用和跨文件一致性；
- 对视觉/物理/复杂架构任务使用中高推理强度；
- 在独立审查存在时，不必每一轮都默认使用最高成本模型。

审查者通常需要：

- 强证据意识、规范对照和边界检查；
- 对复杂回归、架构和交互审查使用高推理强度；
- 保持只读，使能力主要消耗在判断而非实现。

### 2. 项目级显式配置

模型、effort、超时应写在项目运行配置中，由父脚本显式传参。不要静默继承个人上次
交互会话选择，否则一个旧的 max/Opus 选择可能让自动流程意外消耗大量额度。

### 3. 推荐升级策略

```text
常规复杂任务：均衡模型 + high
单轮失败但问题清晰：保持模型，收紧任务和证据
重复两轮仍无法解决：停止自动循环，由人分析原因
确认是能力上限而非规范/工具问题：人工批准更强模型或更高 effort
```

升级模型不能替代明确目标、可执行验收和健康工具链。

## 十、循环上限与终止条件

推荐默认最多三轮，但项目可按风险调整。无论具体数字是多少，都应是显式硬上限。

立即停止并交还所有者的条件：

- PASS；
- 达到最大轮数；
- 连续两轮出现同一未解决的 Blocker/Major；
- 审查要求超出正式范围；
- 需要审美、产品或风险取舍；
- 工作区在阶段边界不干净；
- Agent 越权修改控制面；
- 权限、认证、额度、MCP 或浏览器失败；
- 超时；
- 报告缺失或格式无效；
- 无法安全验证；
- Git 检查点失败；
- 出现未知状态或未知结论。

自动化的目标不是“无论如何继续”，而是“在安全且可判定时继续，在需要人时准确
停止”。

## 十一、故障分类与恢复

### 1. 故障必须结构化

推荐分类：

- `USAGE_OR_BILLING_LIMIT`
- `AUTHENTICATION`
- `PERMISSION`
- `MCP_OR_BROWSER`
- `TIMEOUT`
- `CHILD_PROCESS_ERROR`
- `POLICY_VIOLATION`
- `GIT_CONTROL_PLANE_CHANGED`
- `CHECKPOINT_FAILURE`
- `INVALID_HANDOFF`

停止记录至少包含时间、阶段、分类、退出码和日志路径。

### 2. 失败时保留现场

控制面不应自动 reset、clean 或覆盖上一份有效审查。应：

1. 停止当前阶段；
2. 不启动下一 Agent；
3. 保留日志和未提交文件；
4. 释放活动进程和锁；
5. 生成停止摘要；
6. 让所有者或受授权的基础设施维护者检查。

半成品可以保存为具名、可恢复检查点或 stash，但必须明确标记“未验证”，不能伪装
成有效实现提交。

### 3. 恢复前检查

- 原进程是否真的结束；
- 锁是否活动；
- 工作区和暂存区是否干净；
- HEAD 是否仍是最后有效检查点；
- 上一份审查是否保存；
- 失败原因是否已经解除；
- 当前轮次是否被错误增加；
- 是否需要拆小任务或由人决策。

## 十二、可观测性：让人看得懂数轮发生了什么

原始 Git 历史和完整日志适合追查细节，但不适合所有者快速掌握进展。每次循环结束
或中断后，应自动生成一份简报，按轮回答：

- 实现者改了什么；
- 修改了哪些文件；
- 创建了哪个检查点；
- 验证是否通过；
- 审查者的结论是什么；
- Blocker/Major/Minor 分别是什么；
- 循环为什么停止；
- 所有者接下来应做什么；
- 详细报告和日志在哪里。

可观测性分三层：

```text
状态命令：几十秒内知道现在在哪里
循环简报：几分钟内理解每一轮
Git / 报告 / 原始日志：需要时追查全部证据
```

日志应按任务和轮次命名，不能让下一轮覆盖上一轮。

## 十三、建立新项目的推荐骨架

以下名字可以调整，但职责不应混在一起：

```text
PROJECT.md                     # 技术记忆、架构和历史决策
PROJECT_SPEC.md                # 当前正式目标、范围和验收
AGENTS.md                      # 审查者或通用 Agent 角色规则
IMPLEMENTER.md                 # 实现者规则（也可用工具约定名）
REVIEW_CONTRACT.md             # 审查等级、证据和报告契约

.agent/
  next-task.md                 # 当前执行切片
  state.env                    # 机器可读状态和最大轮数
  runtime.env                  # 模型、effort、超时和心跳
  implementation-report.md     # 最新实现交接
  latest-review.md             # 最新审查
  review-history/              # 只追加审查历史
  artifacts/                   # 日志、截图、验证与循环简报

scripts/
  agent-preflight.sh           # 不启动 Agent 的严格预检
  run-implementation.sh        # 一轮实现包装器
  run-validation.sh            # 统一已配置检查
  run-review.sh                # 一轮只读审查包装器
  agent-cycle.sh               # 有限状态机父入口
  generate-cycle-summary.sh    # 面向人的多轮简报
```

应把本地 artifacts 和锁设为 Git 忽略；正式规范、交接和审查历史是否跟踪，按项目
审计需要决定。

## 十四、标准操作流程

### A. 建立项目

1. 在本地建立独立仓库。
2. 写 `PROJECT.md`，记录项目是什么、技术环境和关键决策。
3. 写当前 `PROJECT_SPEC.md`，包含范围和可验证验收标准。
4. 确定最小角色，不为了“多 Agent”而增加角色。
5. 为每个角色写职责、权限、输入、输出和停止条件。
6. 建立统一验证入口。
7. 建立实现、审查和父循环脚本。
8. 配置最小非交互权限。
9. 用假的短进程测试成功、失败、超时、信号和锁，不立即消耗真实 Agent 额度。
10. 在干净 Git 基线上运行严格预检。

### B. 开始一个新阶段

1. 所有者提出目标和主观期望。
2. 将其提炼为正式范围、非范围和客观验收。
3. 生成唯一任务 ID 和当前执行切片。
4. 将轮次设为 0、状态设为 READY。
5. 检查模型、effort、超时和最大轮数。
6. 提交控制面变更，保证工作区干净。
7. 所有者手动启动一次父循环。

### C. 循环运行

1. 父脚本预检并获取锁。
2. 启动实现者一轮。
3. 验证交接和权限边界。
4. 运行统一验证并创建实现检查点。
5. 启动只读审查者一轮。
6. 验证并归档审查交接，创建审查检查点。
7. PASS 则完成；CHANGES_REQUIRED 且未触发停止条件则回到实现。
8. 始终释放进程和锁，生成简报。

### D. 人的最终验收

1. 先读循环简报。
2. 再体验真实产品。
3. 对感兴趣或有疑问的部分查看详细报告和 Git diff。
4. 接受、提出新阶段目标，或作出审查无法替代的产品决策。
5. 发布由独立、明确授权的流程执行。

## 十五、从 v1.0 项目迁移到 v2.0

### 第一步：盘点现有事实

找出：

- 当前目标写在哪里；
- 项目状态由什么保存；
- 哪些 Agent 能修改哪些文件；
- 谁判断任务完成；
- 是否有统一验证；
- 是否有可追溯检查点；
- 出错后怎样恢复。

### 第二步：停止共享通用身份

选择一个唯一实现者和一个独立审查者。先不要增加更多角色。明确两者的文件权限、
工具权限和交付物。

### 第三步：把目标和审查标准分开

建立正式规范与审查契约。项目历史只提供背景，不再自动成为当前要求。

### 第四步：建立交接面

加入活动任务、机器状态、实现报告、最新审查、历史归档和 artifacts。

### 第五步：建立中立父流程

先支持单轮实现和单轮审查，再组合成有限循环。不要从“让 Agent 互相启动”开始。

### 第六步：加固安全与可观测性

加入独占锁、干净工作区检查、受保护文件检查、超时、故障分类、心跳、模型显式配置
和中文/人类可读简报。

### 第七步：完整演练

至少演练：

- 预检成功；
- 预检失败且没有 Agent 启动；
- 实现者正常完成；
- 实现者额度或权限失败；
- 审查者正常 PASS；
- 审查者要求修改；
- 达到最大轮数；
- `Ctrl+C` 中断；
- 陈旧锁恢复；
- 工作区已有修改时拒绝启动。

## 十六、什么时候不应使用多 Agent

以下任务通常不需要建立完整双 Agent 闭环：

- 一次性、低风险、很小的修改；
- 没有独立验收标准的探索；
- 审查者只能重复实现者的自述；
- 两个 Agent 拥有完全相同的身份、权限和输出；
- 建立控制面的成本明显高于任务本身；
- 人正在学习具体实现过程，希望逐步参与而不是自动交付；
- 项目没有 Git 基线、验证入口或清晰目标。

这时使用一个合适的 Agent，并由人直接检查，往往更简单可靠。

## 十七、判断多 Agent 系统是否真的有效

不要只统计调用次数。应观察：

- 首轮实现后 Major/Blocker 数量；
- 问题是否在下一轮真正关闭；
- 同一问题重复率；
- 每轮从启动到有效检查点的时间；
- 因权限、额度、工具或状态污染造成的失败率；
- 自动停止是否准确；
- 人需要实时介入的次数；
- PASS 后人类主观验收发现的遗漏；
- 单任务额度成本；
- 回滚或恢复是否能定位到精确检查点。

如果多 Agent 只增加轮次和成本，却没有提高缺陷发现、可追溯性或无人值守能力，
应减少角色或退回单 Agent。

## 十八、常见反模式

1. **按品牌分工。** “Claude 做一半、Codex 做一半”不是职责定义。
2. **两个通用写入者。** 二者都可以随意改全部业务文件。
3. **实现者自我验收。** 同一个 Agent 实现后直接宣布完成，没有独立标准。
4. **Agent 互相递归启动。** 生命周期、上限和失败所有权不清楚。
5. **以聊天作为状态。** 任务只存在于最近对话里。
6. **把项目历史当当前规范。** 已推翻路线继续被重新引入。
7. **无人值守却保留询问式权限。** 流程深夜卡在一个弹窗。
8. **为了不询问而授予整机全权。** 自动化获得与任务无关的危险能力。
9. **脏工作区启动。** 无法区分所有者、实现者和旧半成品的修改。
10. **自然语言完成即进入下一步。** 没有检查退出码、报告和 Git 状态。
11. **无限修正循环。** 同一问题反复出现仍自动消耗额度。
12. **验证脚本虚报通过。** 未配置测试被写成 PASS，视觉修改没有真实浏览器检查。
13. **失败后自动清理。** reset/clean 删除了唯一可调查证据。
14. **自动提交等同自动发布。** 未经人类验收就 push 或部署。
15. **只有原始日志，没有简报。** 自动化结束后所有者无法快速理解发生了什么。

## 十九、本次实践形成的参考实现

本方法论的 v2.0 来自一个真实网页项目的协作体系改造。该实践验证了：

- Claude Code 作为唯一业务实现者；
- Codex 作为只读独立审查者；
- 外层 Bash 父脚本严格串行启动两者；
- Agent 不互相启动；
- Git 检查点连接实现与审查；
- 三轮硬上限控制自动纠错；
- 项目级模型和 effort 避免继承昂贵的个人会话选择；
- `dontAsk`/`never` 使未授权能力立即失败而不是卡住；
- 心跳、超时、进程组终止和故障分类支持无人值守；
- 多轮中文简报让所有者不必先阅读完整 Git 历史；
- 额度中断时保留半成品和上一有效检查点，而不伪造成功。

这个实现不是所有项目必须照抄的固定模板。可迁移的是角色分离、控制面独立、单一
写入者、证据交接、有限循环和人在关键决策点接管的原则。

## 二十、v1.0 到 v2.0 的核心变化

| v1.0 | v2.0 |
| --- | --- |
| 按知识型/操作型选择 Agent | 按职责、权限、输入、输出和停止条件定义角色 |
| 多个 Agent 围绕同一项目协作 | 多个不同制度身份通过正式交接协作 |
| 强调项目记忆显式化 | 进一步分离规范、状态、交接、证据和历史 |
| 人选择下一个 Agent | 人启动一次中立父流程 |
| 工作循环主要是概念流程 | 工作循环成为有锁、超时、检查点和上限的状态机 |
| 没有规定谁能修改项目 | 单一实现写入者，审查者默认只读 |
| 没有规定怎样判断 Agent 完成 | 以进程退出、退出码、交付物、验证和 Git 状态共同判断 |
| 没有权限失败策略 | 最小预授权，未授权立即失败并保留证据 |
| 没有自动轮数控制 | PASS、硬上限、重复问题和产品决策都是终止条件 |
| 多 Agent 的价值尚未明确 | 价值来自制度分工与相互制衡，不来自并发或品牌数量 |

## 结语

v1.0 解决了“项目不能依赖某个 AI 的记忆”；v2.0 进一步解决了“多个 AI 进入同一
项目后，怎样形成真正不同、可协作、可自动化又可控制的身份”。

最终原则可以压缩为一句话：

> 让项目保存事实，让人定义目标，让实现者改变状态，让审查者独立判断，让中立控制面
> 管理顺序、权限、证据和停止。
